{
  "date": "2026-09-02",
  "repo": "triton-lang/triton-ascend",
  "branch": "main",
  "commits": [
    {
      "sha": "5377005e8f980a1a8ad8d02fe53b85d1f503c424",
      "author": {
        "name": "Levein",
        "email": "huangyiming25@huawei.com"
      },
      "date": "2026-09-02T16:34:21+08:00",
      "message": "[Docs](feat) update libdevice developer guide docs (#1844)",
      "parents": [
        "645c748bc01d2e46d4e6fb8ecc796f3a088f3fb4"
      ],
      "stats": {
        "total_additions": 3598,
        "total_deletions": 1297,
        "files_changed": 174
      },
      "files": [
        {
          "filename": ".github/docs-ci/codespell.whitelist.json",
          "status": "modified",
          "additions": 1,
          "deletions": 0,
          "patch": "@@ -193,6 +193,7 @@\n   \"powf\",\n   \"sinf\",\n   \"tanf\",\n+  \"tanhf\",\n   \"fdim\",\n   \"finitef\",\n   \"fmod\","
        },
        {
          "filename": "docs/en/libdevice/libdevice_developer_guide.md",
          "status": "modified",
          "additions": 522,
          "deletions": 388,
          "patch": "@@ -2,7 +2,7 @@\n \n ## SIMT Compilation Mode Example\n \n-Triton kernel example with SIMT compilation mode\n+Triton kernel example with SIMT compilation mode. Set `compile_mode='simt_only'` in the kernel launch parameters to enable SIMT-only compilation.\n \n ```python\n # Enable libdevice SIMT compilation\n@@ -25,8 +25,8 @@ def triton_kernel(input, output, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr)\n dtype, shape, ncore, xblock, xblock_sub = ['int32', (128, 4096), 512, 1024, 1024]\n input = torch.randn(shape, dtype=eval('torch.' + dtype)).npu()\n output = torch.zeros_like(input)\n-# Enable SIMT compilation with option \"force_simt_only=True\"\n-triton_kernel[ncore, 1, 1](input, output, xblock, xblock_sub, force_simt_only=True)\n+# Enable SIMT-only compilation with compile_mode='simt_only'\n+triton_kernel[ncore, 1, 1](input, output, xblock, xblock_sub, compile_mode='simt_only')\n ```\n \n ## 1. triton.language.extra.cann.libdevice.abs\n@@ -49,7 +49,7 @@ Return Value: `tl.tensor`, containing the absolute value of the input parameter.\n \n Return Type: `int32`, `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 2. triton.language.extra.cann.libdevice.acos\n \n@@ -71,7 +71,7 @@ Return Value: `tl.tensor`, containing the inverse cosine of the input parameter,\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 3. triton.language.extra.cann.libdevice.acosh\n \n@@ -93,7 +93,7 @@ Return Value: `tl.tensor`, containing the inverse hyperbolic cosine of the input\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 4. triton.language.extra.cann.libdevice.add_rd\n \n@@ -116,7 +116,7 @@ Return Value: `tl.tensor`, containing the addition result rounded down.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 5. triton.language.extra.cann.libdevice.add_rn\n \n@@ -139,7 +139,7 @@ Return Value: `tl.tensor`, containing the addition result rounded to the nearest\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 6. triton.language.extra.cann.libdevice.add_ru\n \n@@ -162,7 +162,7 @@ Return Value: `tl.tensor`, containing the addition result rounded up.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 7. triton.language.extra.cann.libdevice.add_rz\n \n@@ -185,7 +185,7 @@ Return Value: `tl.tensor`, containing the addition result rounded toward zero.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 8. triton.language.extra.cann.libdevice.asin\n \n@@ -207,7 +207,7 @@ Return Value: `tl.tensor`, containing the inverse sine of the input parameter, i\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 9. triton.language.extra.cann.libdevice.asinh\n \n@@ -229,7 +229,7 @@ Return Value: `tl.tensor`, containing the inverse hyperbolic sine of the input p\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 10. triton.language.extra.cann.libdevice.atan\n \n@@ -251,7 +251,7 @@ Return Value: `tl.tensor`, containing the inverse tangent of the input parameter\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 11. triton.language.extra.cann.libdevice.atan2\n \n@@ -274,7 +274,7 @@ Return Value: `tl.tensor`, containing the arctangent of x / y, in the range \\[-\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 12. triton.language.extra.cann.libdevice.atanh\n \n@@ -296,7 +296,7 @@ Return Value: `tl.tensor`, containing the inverse hyperbolic tangent of the inpu\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 13. triton.language.extra.cann.libdevice.brev\n \n@@ -318,7 +318,7 @@ Return Value: `tl.tensor`, containing the 32-bit integer with reversed bit order\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 14. triton.language.extra.cann.libdevice.byte_perm\n \n@@ -356,7 +356,7 @@ Return Value: `tl.tensor`, where return[n] := input[selector[n]], where n repres\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 15. triton.language.extra.cann.libdevice.cbrt\n \n@@ -378,7 +378,7 @@ Return Value: `tl.tensor`, containing the cube root of the input parameter.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 16. triton.language.extra.cann.libdevice.ceil\n \n@@ -400,7 +400,7 @@ Return Value: `tl.tensor`, containing the ceiling result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 17. triton.language.extra.cann.libdevice.clz\n \n@@ -422,7 +422,7 @@ Return Value: `tl.tensor`, containing the number of leading zeros in the input p\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 18. triton.language.extra.cann.libdevice.copysign\n \n@@ -445,7 +445,7 @@ Return Value: `tl.tensor`, containing a floating-point number with magnitude equ\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 19. triton.language.extra.cann.libdevice.cos\n \n@@ -467,7 +467,7 @@ Return Value: `tl.tensor`, containing the cosine of the input parameter.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 20. triton.language.extra.cann.libdevice.cosh\n \n@@ -489,7 +489,7 @@ Return Value: `tl.tensor`, containing the hyperbolic cosine of the input paramet\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 21. triton.language.extra.cann.libdevice.cospi\n \n@@ -511,7 +511,7 @@ Return Value: `tl.tensor`, containing the value of cos(π × x).\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 22. triton.language.extra.cann.libdevice.cyl_bessel_i0\n \n@@ -533,7 +533,7 @@ Return Value: `tl.tensor`, containing the modified Bessel function of the first\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 23. triton.language.extra.cann.libdevice.cyl_bessel_i1\n \n@@ -555,7 +555,7 @@ Return Value: `tl.tensor`, containing the modified Bessel function of the first\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 24. triton.language.extra.cann.libdevice.div_rd\n \n@@ -578,7 +578,7 @@ Return Value: `tl.tensor`, containing the division result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 25. triton.language.extra.cann.libdevice.div_rn\n \n@@ -601,7 +601,7 @@ Return Value: `tl.tensor`, containing the division result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 26. triton.language.extra.cann.libdevice.div_ru\n \n@@ -624,7 +624,7 @@ Return Value: `tl.tensor`, containing the division result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 27. triton.language.extra.cann.libdevice.div_rz\n \n@@ -647,7 +647,7 @@ Return Value: `tl.tensor`, containing the division result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 28. triton.language.extra.cann.libdevice.erf\n \n@@ -669,7 +669,7 @@ Return Value: `tl.tensor`, containing the error function of the input parameter.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 29. triton.language.extra.cann.libdevice.erfc\n \n@@ -691,7 +691,7 @@ Return Value: `tl.tensor`, containing the complementary error function of the in\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 30. triton.language.extra.cann.libdevice.erfcinv\n \n@@ -713,7 +713,7 @@ Return Value: `tl.tensor`, containing the inverse complementary error function o\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 31. triton.language.extra.cann.libdevice.erfcx\n \n@@ -735,7 +735,7 @@ Return Value: `tl.tensor`, containing the scaled complementary error function of\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 32. triton.language.extra.cann.libdevice.erfinv\n \n@@ -757,7 +757,7 @@ Return Value: `tl.tensor`, containing the inverse error function of the input pa\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 33. triton.language.extra.cann.libdevice.exp\n \n@@ -779,7 +779,7 @@ Return Value: `tl.tensor`, containing the result of e raised to the power of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 34. triton.language.extra.cann.libdevice.exp10\n \n@@ -801,7 +801,7 @@ Return Value: `tl.tensor`, containing the result of 10 raised to the power of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 35. triton.language.extra.cann.libdevice.exp2\n \n@@ -823,7 +823,7 @@ Return Value: `tl.tensor`, containing the result of 2 raised to the power of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 36. triton.language.extra.cann.libdevice.expm1\n \n@@ -845,7 +845,7 @@ Return Value: `tl.tensor`, containing the result of e raised to the power of x,\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 37. triton.language.extra.cann.libdevice.fast_cosf\n \n@@ -867,7 +867,7 @@ Return Value: `tl.tensor`, containing the result of the fast approximate cosine\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 38. triton.language.extra.cann.libdevice.fast_dividef\n \n@@ -890,117 +890,117 @@ Return Value: `tl.tensor`, containing the result of fast approximate division.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 39. triton.language.extra.cann.libdevice.fast_exp10f\n+## 39. triton.language.extra.cann.libdevice.fast_expf\n \n ### OP Overview\n \n-Fast approximate base-10 exponential function.\n+Fast approximate exponential function.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_exp10f(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_expf(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `float32`\n \n-Return Value: `tl.tensor`, containing the result of the fast approximate base-10 exponential function.\n+Return Value: `tl.tensor`, containing the result of the fast approximate exponential function.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 40. triton.language.extra.cann.libdevice.fast_expf\n+## 40. triton.language.extra.cann.libdevice.fast_exp10f\n \n ### OP Overview\n \n-Fast approximate exponential function.\n+Fast approximate base-10 exponential function.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_expf(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_exp10f(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `float32`\n \n-Return Value: `tl.tensor`, containing the result of the fast approximate exponential function.\n+Return Value: `tl.tensor`, containing the result of the fast approximate base-10 exponential function.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 41. triton.language.extra.cann.libdevice.fast_log10f\n+## 41. triton.language.extra.cann.libdevice.fast_logf\n \n ### OP Overview\n \n-Fast approximate base-10 logarithm function.\n+Fast approximate natural logarithm function.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_log10f(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_logf(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `float32`\n \n-Return Value: `tl.tensor`, containing the result of the fast approximate base-10 logarithm function.\n+Return Value: `tl.tensor`, containing the result of the fast approximate natural logarithm function.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 42. triton.language.extra.cann.libdevice.fast_log2f\n+## 42. triton.language.extra.cann.libdevice.fast_log10f\n \n ### OP Overview\n \n-Fast approximate base-2 logarithm function.\n+Fast approximate base-10 logarithm function.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_log2f(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_log10f(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `float32`\n \n-Return Value: `tl.tensor`, containing the result of the fast approximate base-2 logarithm function.\n+Return Value: `tl.tensor`, containing the result of the fast approximate base-10 logarithm function.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 43. triton.language.extra.cann.libdevice.fast_logf\n+## 43. triton.language.extra.cann.libdevice.fast_log2f\n \n ### OP Overview\n \n-Fast approximate natural logarithm function.\n+Fast approximate base-2 logarithm function.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_logf(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_log2f(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `float32`\n \n-Return Value: `tl.tensor`, containing the result of the fast approximate natural logarithm function.\n+Return Value: `tl.tensor`, containing the result of the fast approximate base-2 logarithm function.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 44. triton.language.extra.cann.libdevice.fast_powf\n \n@@ -1023,7 +1023,7 @@ Return Value: `tl.tensor`, containing the result of fast approximate power funct\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 45. triton.language.extra.cann.libdevice.fast_sinf\n \n@@ -1045,7 +1045,7 @@ Return Value: `tl.tensor`, containing the result of the fast approximate sine fu\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n ## 46. triton.language.extra.cann.libdevice.fast_tanf\n \n@@ -1067,9 +1067,31 @@ Return Value: `tl.tensor`, containing the result of the fast approximate tangent\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 47. triton.language.extra.cann.libdevice.fast_tanhf\n+\n+### OP Overview\n+\n+Computes the hyperbolic tangent of x using a fast approximation.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.fast_tanhf(x, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `float32`\n+\n+Return Value: `tl.tensor`, containing the fast approximate hyperbolic tangent of x.\n+\n+Return Type: `float32`\n+\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 47. triton.language.extra.cann.libdevice.fdim\n+## 48. triton.language.extra.cann.libdevice.fdim\n \n ### OP Overview\n \n@@ -1090,9 +1112,9 @@ Return Value: `tl.tensor`, containing the positive difference between x and y.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 48. triton.language.extra.cann.libdevice.ffs\n+## 49. triton.language.extra.cann.libdevice.ffs\n \n ### OP Overview\n \n@@ -1112,9 +1134,9 @@ Return Value: `tl.tensor`, containing the index of the lowest bit set to 1. Rang\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 49. triton.language.extra.cann.libdevice.finitef\n+## 50. triton.language.extra.cann.libdevice.finitef\n \n ### OP Overview\n \n@@ -1134,9 +1156,75 @@ Return Value: `tl.tensor`, returns True if the input is finite, otherwise return\n \n Return Type: `bool`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 50. triton.language.extra.cann.libdevice.float2int_rd\n+## 51. triton.language.extra.cann.libdevice.float_as_int\n+\n+### OP Overview\n+\n+Reinterprets the bit pattern of a floating-point number as a 32-bit integer. No numeric conversion is performed.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.float_as_int(x, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `float32`\n+\n+Return Value: `tl.tensor`, containing the bit pattern of the floating-point number reinterpreted as a 32-bit integer.\n+\n+Return Type: `int32`\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 52. triton.language.extra.cann.libdevice.float_as_uint\n+\n+### OP Overview\n+\n+Reinterprets the bit pattern of a floating-point number as a 32-bit unsigned integer. No numeric conversion is performed.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.float_as_uint(x, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `float32`\n+\n+Return Value: `tl.tensor`, containing the bit pattern of the floating-point number reinterpreted as a 32-bit unsigned integer.\n+\n+Return Type: `uint32`\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 53. triton.language.extra.cann.libdevice.float2half_rn\n+\n+### OP Overview\n+\n+Converts x from a 32-bit floating-point value to a 16-bit floating-point value using round-to-nearest-even.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.float2half_rn(x, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `float32`\n+\n+Return Value: `tl.tensor`, containing the converted 16-bit floating-point value.\n+\n+Return Type: `float16`\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 54. triton.language.extra.cann.libdevice.float2int_rd\n \n ### OP Overview\n \n@@ -1156,9 +1244,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit integer.\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 51. triton.language.extra.cann.libdevice.float2int_rn\n+## 55. triton.language.extra.cann.libdevice.float2int_rn\n \n ### OP Overview\n \n@@ -1178,9 +1266,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit integer.\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 52. triton.language.extra.cann.libdevice.float2int_ru\n+## 56. triton.language.extra.cann.libdevice.float2int_ru\n \n ### OP Overview\n \n@@ -1200,9 +1288,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit integer.\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 53. triton.language.extra.cann.libdevice.float2int_rz\n+## 57. triton.language.extra.cann.libdevice.float2int_rz\n \n ### OP Overview\n \n@@ -1222,9 +1310,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit integer.\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 54. triton.language.extra.cann.libdevice.float2ll_rd\n+## 58. triton.language.extra.cann.libdevice.float2ll_rd\n \n ### OP Overview\n \n@@ -1244,9 +1332,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit integer.\n \n Return Type: `int64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 55. triton.language.extra.cann.libdevice.float2ll_rn\n+## 59. triton.language.extra.cann.libdevice.float2ll_rn\n \n ### OP Overview\n \n@@ -1266,9 +1354,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit integer.\n \n Return Type: `int64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 56. triton.language.extra.cann.libdevice.float2ll_ru\n+## 60. triton.language.extra.cann.libdevice.float2ll_ru\n \n ### OP Overview\n \n@@ -1288,9 +1376,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit integer.\n \n Return Type: `int64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 57. triton.language.extra.cann.libdevice.float2ll_rz\n+## 61. triton.language.extra.cann.libdevice.float2ll_rz\n \n ### OP Overview\n \n@@ -1310,9 +1398,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit integer.\n \n Return Type: `int64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 58. triton.language.extra.cann.libdevice.float2uint_rd\n+## 62. triton.language.extra.cann.libdevice.float2uint_rd\n \n ### OP Overview\n \n@@ -1332,9 +1420,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit unsigned integer.\n \n Return Type: `uint32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 59. triton.language.extra.cann.libdevice.float2uint_rn\n+## 63. triton.language.extra.cann.libdevice.float2uint_rn\n \n ### OP Overview\n \n@@ -1354,9 +1442,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit unsigned integer.\n \n Return Type: `uint32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 60. triton.language.extra.cann.libdevice.float2uint_ru\n+## 64. triton.language.extra.cann.libdevice.float2uint_ru\n \n ### OP Overview\n \n@@ -1376,9 +1464,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit unsigned integer.\n \n Return Type: `uint32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 61. triton.language.extra.cann.libdevice.float2uint_rz\n+## 65. triton.language.extra.cann.libdevice.float2uint_rz\n \n ### OP Overview\n \n@@ -1398,9 +1486,9 @@ Return Value: `tl.tensor`, containing the converted 32-bit unsigned integer.\n \n Return Type: `uint32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 62. triton.language.extra.cann.libdevice.float2ull_rd\n+## 66. triton.language.extra.cann.libdevice.float2ull_rd\n \n ### OP Overview\n \n@@ -1420,9 +1508,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit unsigned integer.\n \n Return Type: `uint64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 63. triton.language.extra.cann.libdevice.float2ull_rn\n+## 67. triton.language.extra.cann.libdevice.float2ull_rn\n \n ### OP Overview\n \n@@ -1442,9 +1530,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit unsigned integer.\n \n Return Type: `uint64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 64. triton.language.extra.cann.libdevice.float2ull_ru\n+## 68. triton.language.extra.cann.libdevice.float2ull_ru\n \n ### OP Overview\n \n@@ -1464,9 +1552,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit unsigned integer.\n \n Return Type: `uint64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 65. triton.language.extra.cann.libdevice.float2ull_rz\n+## 69. triton.language.extra.cann.libdevice.float2ull_rz\n \n ### OP Overview\n \n@@ -1486,53 +1574,9 @@ Return Value: `tl.tensor`, containing the converted 64-bit unsigned integer.\n \n Return Type: `uint64`\n \n-Supported Compilation Mode: SIMT\n-\n-## 66. triton.language.extra.cann.libdevice.float_as_int\n-\n-### OP Overview\n-\n-Reinterprets the bit pattern of a floating-point number as a 32-bit integer. No numeric conversion is performed.\n-\n-Prototype:\n-\n-```python\n-triton.language.extra.cann.libdevice.float_as_int(x, _builder=None)\n-```\n-\n-Input Types:\n-\n-- x: `float32`\n-\n-Return Value: `tl.tensor`, containing the bit pattern of the floating-point number reinterpreted as a 32-bit integer.\n-\n-Return Type: `int32`\n-\n-Supported Compilation Mode: SIMT, SIMD\n-\n-## 67. triton.language.extra.cann.libdevice.float_as_uint\n-\n-### OP Overview\n-\n-Reinterprets the bit pattern of a floating-point number as a 32-bit unsigned integer. No numeric conversion is performed.\n-\n-Prototype:\n-\n-```python\n-triton.language.extra.cann.libdevice.float_as_uint(x, _builder=None)\n-```\n-\n-Input Types:\n-\n-- x: `float32`\n-\n-Return Value: `tl.tensor`, containing the bit pattern of the floating-point number reinterpreted as a 32-bit unsigned integer.\n-\n-Return Type: `uint32`\n+Supported Compilation Mode: SIMD, SIMT\n \n-Supported Compilation Mode: SIMT\n-\n-## 68. triton.language.extra.cann.libdevice.floor\n+## 70. triton.language.extra.cann.libdevice.floor\n \n ### OP Overview\n \n@@ -1552,9 +1596,9 @@ Return Value: `tl.tensor`, containing the floor result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 69. triton.language.extra.cann.libdevice.fma\n+## 71. triton.language.extra.cann.libdevice.fma\n \n ### OP Overview\n \n@@ -1576,9 +1620,9 @@ Return Value: `tl.tensor`, containing the result of fused multiply-add.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 70. triton.language.extra.cann.libdevice.fma_rd\n+## 72. triton.language.extra.cann.libdevice.fma_rd\n \n ### OP Overview\n \n@@ -1600,9 +1644,9 @@ Return Value: `tl.tensor`, containing the result of fused multiply-add.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 71. triton.language.extra.cann.libdevice.fma_rn\n+## 73. triton.language.extra.cann.libdevice.fma_rn\n \n ### OP Overview\n \n@@ -1624,9 +1668,9 @@ Return Value: `tl.tensor`, containing the result of fused multiply-add.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 72. triton.language.extra.cann.libdevice.fma_ru\n+## 74. triton.language.extra.cann.libdevice.fma_ru\n \n ### OP Overview\n \n@@ -1648,9 +1692,9 @@ Return Value: `tl.tensor`, containing the result of fused multiply-add.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 73. triton.language.extra.cann.libdevice.fma_rz\n+## 75. triton.language.extra.cann.libdevice.fma_rz\n \n ### OP Overview\n \n@@ -1672,9 +1716,9 @@ Return Value: `tl.tensor`, containing the result of fused multiply-add.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 74. triton.language.extra.cann.libdevice.fmod\n+## 76. triton.language.extra.cann.libdevice.fmod\n \n ### OP Overview\n \n@@ -1695,9 +1739,9 @@ Return Value: `tl.tensor`, containing the floating-point modulo result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 75. triton.language.extra.cann.libdevice.gamma\n+## 77. triton.language.extra.cann.libdevice.gamma\n \n ### OP Overview\n \n@@ -1717,9 +1761,9 @@ Return Value: `tl.tensor`, containing the gamma function of the input parameter.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 76. triton.language.extra.cann.libdevice.hadd\n+## 78. triton.language.extra.cann.libdevice.hadd\n \n ### OP Overview\n \n@@ -1733,16 +1777,38 @@ triton.language.extra.cann.libdevice.hadd(x, y, _builder=None)\n \n Input Types:\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32` or `uint32`\n+- y: `int32` or `uint32`; must have the same type as x\n \n Return Value: `tl.tensor`, containing the average of x and y.\n \n-Return Type: `int32`\n+Return Type: Same as the input type (`int32` or `uint32`).\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 77. triton.language.extra.cann.libdevice.hypot\n+## 79. triton.language.extra.cann.libdevice.half2float\n+\n+### OP Overview\n+\n+Converts x from a 16-bit floating-point value to a 32-bit floating-point value.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.half2float(x, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `float16`\n+\n+Return Value: `tl.tensor`, containing the converted 32-bit floating-point value.\n+\n+Return Type: `float32`\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 80. triton.language.extra.cann.libdevice.hypot\n \n ### OP Overview\n \n@@ -1763,9 +1829,9 @@ Return Value: `tl.tensor`, containing the Euclidean distance between x and y.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 78. triton.language.extra.cann.libdevice.ilogb\n+## 81. triton.language.extra.cann.libdevice.ilogb\n \n ### OP Overview\n \n@@ -1785,40 +1851,40 @@ Return Value: `tl.tensor`, containing the unbiased exponent of the input paramet\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 79. triton.language.extra.cann.libdevice.int2float_rd\n+## 82. triton.language.extra.cann.libdevice.int_as_float\n \n ### OP Overview\n \n-Converts a 32-bit integer to a floating-point number with round-down mode.\n+Reinterprets the bit pattern of a 32-bit integer as a floating-point number. No numeric conversion is performed.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_rd(x, _builder=None)\n+triton.language.extra.cann.libdevice.int_as_float(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `int32`\n \n-Return Value: `tl.tensor`, containing the converted floating-point number.\n+Return Value: `tl.tensor`, containing the bit pattern of the 32-bit integer reinterpreted as a floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 80. triton.language.extra.cann.libdevice.int2float_rn\n+## 83. triton.language.extra.cann.libdevice.int2float_rd\n \n ### OP Overview\n \n-Converts a 32-bit integer to a floating-point number with round-to-nearest-even mode.\n+Converts a 32-bit integer to a floating-point number with round-down mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_rn(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_rd(x, _builder=None)\n ```\n \n Input Types:\n@@ -1829,18 +1895,18 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 81. triton.language.extra.cann.libdevice.int2float_ru\n+## 84. triton.language.extra.cann.libdevice.int2float_rn\n \n ### OP Overview\n \n-Converts a 32-bit integer to a floating-point number with round-up mode.\n+Converts a 32-bit integer to a floating-point number with round-to-nearest-even mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_ru(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_rn(x, _builder=None)\n ```\n \n Input Types:\n@@ -1851,18 +1917,18 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 82. triton.language.extra.cann.libdevice.int2float_rz\n+## 85. triton.language.extra.cann.libdevice.int2float_ru\n \n ### OP Overview\n \n-Converts a 32-bit integer to a floating-point number with round-toward-zero mode.\n+Converts a 32-bit integer to a floating-point number with round-up mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_rz(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_ru(x, _builder=None)\n ```\n \n Input Types:\n@@ -1873,31 +1939,31 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 83. triton.language.extra.cann.libdevice.int_as_float\n+## 86. triton.language.extra.cann.libdevice.int2float_rz\n \n ### OP Overview\n \n-Reinterprets the bit pattern of a 32-bit integer as a floating-point number. No numeric conversion is performed.\n+Converts a 32-bit integer to a floating-point number with round-toward-zero mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.int_as_float(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_rz(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `int32`\n \n-Return Value: `tl.tensor`, containing the bit pattern of the 32-bit integer reinterpreted as a floating-point number.\n+Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 84. triton.language.extra.cann.libdevice.isinf\n+## 87. triton.language.extra.cann.libdevice.isinf\n \n ### OP Overview\n \n@@ -1917,9 +1983,9 @@ Return Value: `tl.tensor`, returns True if the input is infinity, otherwise retu\n \n Return Type: `bool`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 85. triton.language.extra.cann.libdevice.isnan\n+## 88. triton.language.extra.cann.libdevice.isnan\n \n ### OP Overview\n \n@@ -1939,9 +2005,9 @@ Return Value: `tl.tensor`, returns True if the input is NaN, otherwise returns F\n \n Return Type: `bool`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 86. triton.language.extra.cann.libdevice.j0\n+## 89. triton.language.extra.cann.libdevice.j0\n \n ### OP Overview\n \n@@ -1961,9 +2027,9 @@ Return Value: `tl.tensor`, containing the Bessel function of the first kind of o\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 87. triton.language.extra.cann.libdevice.j1\n+## 90. triton.language.extra.cann.libdevice.j1\n \n ### OP Overview\n \n@@ -1983,9 +2049,9 @@ Return Value: `tl.tensor`, containing the Bessel function of the first kind of o\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 88. triton.language.extra.cann.libdevice.jn\n+## 91. triton.language.extra.cann.libdevice.jn\n \n ### OP Overview\n \n@@ -2006,9 +2072,9 @@ Return Value: `tl.tensor`, containing the Bessel function of the first kind of o\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 89. triton.language.extra.cann.libdevice.ldexp\n+## 92. triton.language.extra.cann.libdevice.ldexp\n \n ### OP Overview\n \n@@ -2029,9 +2095,9 @@ Return Value: `tl.tensor`, containing the result of x × 2^exp.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 90. triton.language.extra.cann.libdevice.lgamma\n+## 93. triton.language.extra.cann.libdevice.lgamma\n \n ### OP Overview\n \n@@ -2051,9 +2117,9 @@ Return Value: `tl.tensor`, containing the natural logarithm of the absolute valu\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 91. triton.language.extra.cann.libdevice.ll2float_rd\n+## 94. triton.language.extra.cann.libdevice.ll2float_rd\n \n ### OP Overview\n \n@@ -2073,9 +2139,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 92. triton.language.extra.cann.libdevice.ll2float_rn\n+## 95. triton.language.extra.cann.libdevice.ll2float_rn\n \n ### OP Overview\n \n@@ -2095,9 +2161,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 93. triton.language.extra.cann.libdevice.ll2float_ru\n+## 96. triton.language.extra.cann.libdevice.ll2float_ru\n \n ### OP Overview\n \n@@ -2117,9 +2183,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 94. triton.language.extra.cann.libdevice.ll2float_rz\n+## 97. triton.language.extra.cann.libdevice.ll2float_rz\n \n ### OP Overview\n \n@@ -2139,9 +2205,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 95. triton.language.extra.cann.libdevice.llrint\n+## 98. triton.language.extra.cann.libdevice.llrint\n \n ### OP Overview\n \n@@ -2161,9 +2227,9 @@ Return Value: `tl.tensor`, containing the rounded 64-bit integer.\n \n Return Type: `int64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 96. triton.language.extra.cann.libdevice.llround\n+## 99. triton.language.extra.cann.libdevice.llround\n \n ### OP Overview\n \n@@ -2183,9 +2249,9 @@ Return Value: `tl.tensor`, containing the rounded 64-bit integer.\n \n Return Type: `int64`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 97. triton.language.extra.cann.libdevice.log\n+## 100. triton.language.extra.cann.libdevice.log\n \n ### OP Overview\n \n@@ -2205,9 +2271,9 @@ Return Value: `tl.tensor`, containing the natural logarithm of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 98. triton.language.extra.cann.libdevice.log10\n+## 101. triton.language.extra.cann.libdevice.log10\n \n ### OP Overview\n \n@@ -2227,9 +2293,9 @@ Return Value: `tl.tensor`, containing the base-10 logarithm of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 99. triton.language.extra.cann.libdevice.log1p\n+## 102. triton.language.extra.cann.libdevice.log1p\n \n ### OP Overview\n \n@@ -2249,9 +2315,9 @@ Return Value: `tl.tensor`, containing the result of log(1 + x).\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 100. triton.language.extra.cann.libdevice.log2\n+## 103. triton.language.extra.cann.libdevice.log2\n \n ### OP Overview\n \n@@ -2271,9 +2337,9 @@ Return Value: `tl.tensor`, containing the base-2 logarithm of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 101. triton.language.extra.cann.libdevice.logb\n+## 104. triton.language.extra.cann.libdevice.logb\n \n ### OP Overview\n \n@@ -2293,32 +2359,55 @@ Return Value: `tl.tensor`, containing the exponent value of the input parameter.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 102. triton.language.extra.cann.libdevice.mul24\n+## 105. triton.language.extra.cann.libdevice.max\n \n ### OP Overview\n \n-Computes the lower 24-bit multiplication result of x and y.\n+Computes the element-wise maximum of x and y.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.mul24(x, y, _builder=None)\n+triton.language.extra.cann.libdevice.max(x, y, _builder=None)\n ```\n \n Input Types:\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32`, `float32`\n+- y: `int32`, `float32`; must have the same type as x\n \n-Return Value: `tl.tensor`, containing the lower 24-bit multiplication result of x and y.\n+Return Value: `tl.tensor`, containing the element-wise maximum of x and y.\n \n-Return Type: `int32`\n+Return Type: Same as the input type (`int32` or `float32`).\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 106. triton.language.extra.cann.libdevice.min\n+\n+### OP Overview\n+\n+Computes the element-wise minimum of x and y.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.min(x, y, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `int32`, `float32`\n+- y: `int32`, `float32`; must have the same type as x\n+\n+Return Value: `tl.tensor`, containing the element-wise minimum of x and y.\n+\n+Return Type: Same as the input type (`int32` or `float32`).\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 103. triton.language.extra.cann.libdevice.mul_rd\n+## 107. triton.language.extra.cann.libdevice.mul_rd\n \n ### OP Overview\n \n@@ -2339,9 +2428,9 @@ Return Value: `tl.tensor`, containing the floating-point multiplication result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 104. triton.language.extra.cann.libdevice.mul_rn\n+## 108. triton.language.extra.cann.libdevice.mul_rn\n \n ### OP Overview\n \n@@ -2362,9 +2451,9 @@ Return Value: `tl.tensor`, containing the floating-point multiplication result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 105. triton.language.extra.cann.libdevice.mul_ru\n+## 109. triton.language.extra.cann.libdevice.mul_ru\n \n ### OP Overview\n \n@@ -2385,9 +2474,9 @@ Return Value: `tl.tensor`, containing the floating-point multiplication result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 106. triton.language.extra.cann.libdevice.mul_rz\n+## 110. triton.language.extra.cann.libdevice.mul_rz\n \n ### OP Overview\n \n@@ -2408,9 +2497,32 @@ Return Value: `tl.tensor`, containing the floating-point multiplication result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 107. triton.language.extra.cann.libdevice.mulhi\n+## 111. triton.language.extra.cann.libdevice.mul24\n+\n+### OP Overview\n+\n+Computes the lower 24-bit multiplication result of x and y.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.mul24(x, y, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `int32` or `uint32`\n+- y: `int32` or `uint32`; must have the same type as x\n+\n+Return Value: `tl.tensor`, containing the lower 24-bit multiplication result of x and y.\n+\n+Return Type: Same as the input type (`int32` or `uint32`).\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 112. triton.language.extra.cann.libdevice.mulhi\n \n ### OP Overview\n \n@@ -2424,16 +2536,38 @@ triton.language.extra.cann.libdevice.mulhi(x, y, _builder=None)\n \n Input Types:\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32` or `uint32`\n+- y: `int32` or `uint32`; must have the same type as x\n \n Return Value: `tl.tensor`, containing the high 32 bits of the multiplication result of x and y.\n \n-Return Type: `int32`\n+Return Type: Same as the input type (`int32` or `uint32`).\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 108. triton.language.extra.cann.libdevice.nearbyint\n+## 113. triton.language.extra.cann.libdevice.nan\n+\n+### OP Overview\n+\n+Generates a NaN value from x.\n+\n+Prototype:\n+\n+```python\n+triton.language.extra.cann.libdevice.nan(x, _builder=None)\n+```\n+\n+Input Types:\n+\n+- x: `float32`\n+\n+Return Value: `tl.tensor`, containing the generated NaN value.\n+\n+Return Type: `float32`\n+\n+Supported Compilation Mode: SIMD, SIMT\n+\n+## 114. triton.language.extra.cann.libdevice.nearbyint\n \n ### OP Overview\n \n@@ -2453,9 +2587,9 @@ Return Value: `tl.tensor`, containing the nearest integer.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 109. triton.language.extra.cann.libdevice.nextafter\n+## 115. triton.language.extra.cann.libdevice.nextafter\n \n ### OP Overview\n \n@@ -2476,9 +2610,9 @@ Return Value: `tl.tensor`, containing the next representable floating-point numb\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 110. triton.language.extra.cann.libdevice.norm3d\n+## 116. triton.language.extra.cann.libdevice.norm3d\n \n ### OP Overview\n \n@@ -2500,9 +2634,9 @@ Return Value: `tl.tensor`, containing the Euclidean norm of the 3D vector.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 111. triton.language.extra.cann.libdevice.norm4d\n+## 117. triton.language.extra.cann.libdevice.norm4d\n \n ### OP Overview\n \n@@ -2525,9 +2659,9 @@ Return Value: `tl.tensor`, containing the Euclidean norm of the 4D vector.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 112. triton.language.extra.cann.libdevice.normcdf\n+## 118. triton.language.extra.cann.libdevice.normcdf\n \n ### OP Overview\n \n@@ -2547,9 +2681,9 @@ Return Value: `tl.tensor`, containing the cumulative distribution function of th\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 113. triton.language.extra.cann.libdevice.normcdfinv\n+## 119. triton.language.extra.cann.libdevice.normcdfinv\n \n ### OP Overview\n \n@@ -2569,9 +2703,9 @@ Return Value: `tl.tensor`, containing the inverse of the cumulative distribution\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 114. triton.language.extra.cann.libdevice.popc\n+## 120. triton.language.extra.cann.libdevice.popc\n \n ### OP Overview\n \n@@ -2591,9 +2725,9 @@ Return Value: `tl.tensor`, containing the number of bits set to 1 in x. Range: \\\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 115. triton.language.extra.cann.libdevice.pow\n+## 121. triton.language.extra.cann.libdevice.pow\n \n ### OP Overview\n \n@@ -2614,9 +2748,9 @@ Return Value: `tl.tensor`, containing x raised to the power of y.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 116. triton.language.extra.cann.libdevice.rcbrt\n+## 122. triton.language.extra.cann.libdevice.rcbrt\n \n ### OP Overview\n \n@@ -2636,9 +2770,9 @@ Return Value: `tl.tensor`, containing the reciprocal cube root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 117. triton.language.extra.cann.libdevice.rcp_rd\n+## 123. triton.language.extra.cann.libdevice.rcp_rd\n \n ### OP Overview\n \n@@ -2658,9 +2792,9 @@ Return Value: `tl.tensor`, containing 1 / x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 118. triton.language.extra.cann.libdevice.rcp_rn\n+## 124. triton.language.extra.cann.libdevice.rcp_rn\n \n ### OP Overview\n \n@@ -2680,9 +2814,9 @@ Return Value: `tl.tensor`, containing 1 / x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 119. triton.language.extra.cann.libdevice.rcp_ru\n+## 125. triton.language.extra.cann.libdevice.rcp_ru\n \n ### OP Overview\n \n@@ -2702,9 +2836,9 @@ Return Value: `tl.tensor`, containing 1 / x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 120. triton.language.extra.cann.libdevice.rcp_rz\n+## 126. triton.language.extra.cann.libdevice.rcp_rz\n \n ### OP Overview\n \n@@ -2724,9 +2858,9 @@ Return Value: `tl.tensor`, containing 1 / x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 121. triton.language.extra.cann.libdevice.reciprocal\n+## 127. triton.language.extra.cann.libdevice.reciprocal\n \n ### OP Overview\n \n@@ -2746,9 +2880,9 @@ Return Value: `tl.tensor`, containing 1 / x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 122. triton.language.extra.cann.libdevice.relu\n+## 128. triton.language.extra.cann.libdevice.relu\n \n ### OP Overview\n \n@@ -2768,9 +2902,9 @@ Return Value: `tl.tensor`, containing the result of the rectified linear unit.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 123. triton.language.extra.cann.libdevice.remainder\n+## 129. triton.language.extra.cann.libdevice.remainder\n \n ### OP Overview\n \n@@ -2791,9 +2925,9 @@ Return Value: `tl.tensor`, containing the remainder of x divided by y.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 124. triton.language.extra.cann.libdevice.rhadd\n+## 130. triton.language.extra.cann.libdevice.rhadd\n \n ### OP Overview\n \n@@ -2807,16 +2941,16 @@ triton.language.extra.cann.libdevice.rhadd(x, y, _builder=None)\n \n Input Types:\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32` or `uint32`\n+- y: `int32` or `uint32`; must have the same type as x\n \n Return Value: `tl.tensor`, containing the rounded average of x and y.\n \n-Return Type: `int32`\n+Return Type: Same as the input type (`int32` or `uint32`).\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 125. triton.language.extra.cann.libdevice.rhypot\n+## 131. triton.language.extra.cann.libdevice.rhypot\n \n ### OP Overview\n \n@@ -2837,9 +2971,9 @@ Return Value: `tl.tensor`, containing the reciprocal of the Euclidean distance b\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 126. triton.language.extra.cann.libdevice.rint\n+## 132. triton.language.extra.cann.libdevice.rint\n \n ### OP Overview\n \n@@ -2859,9 +2993,9 @@ Return Value: `tl.tensor`, containing the nearest integer to x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 127. triton.language.extra.cann.libdevice.rnorm3d\n+## 133. triton.language.extra.cann.libdevice.rnorm3d\n \n ### OP Overview\n \n@@ -2883,9 +3017,9 @@ Return Value: `tl.tensor`, containing the reciprocal of the Euclidean norm of th\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 128. triton.language.extra.cann.libdevice.rnorm4d\n+## 134. triton.language.extra.cann.libdevice.rnorm4d\n \n ### OP Overview\n \n@@ -2908,9 +3042,9 @@ Return Value: `tl.tensor`, containing the reciprocal of the Euclidean norm of th\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 129. triton.language.extra.cann.libdevice.round\n+## 135. triton.language.extra.cann.libdevice.round\n \n ### OP Overview\n \n@@ -2930,9 +3064,9 @@ Return Value: `tl.tensor`, containing the nearest integer to x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 130. triton.language.extra.cann.libdevice.rsqrt\n+## 136. triton.language.extra.cann.libdevice.rsqrt\n \n ### OP Overview\n \n@@ -2952,9 +3086,9 @@ Return Value: `tl.tensor`, containing the reciprocal square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 131. triton.language.extra.cann.libdevice.rsqrt_rn\n+## 137. triton.language.extra.cann.libdevice.rsqrt_rn\n \n ### OP Overview\n \n@@ -2974,13 +3108,13 @@ Return Value: `tl.tensor`, containing the reciprocal square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 132. triton.language.extra.cann.libdevice.sad\n+## 138. triton.language.extra.cann.libdevice.sad\n \n ### OP Overview\n \n-Computes |x-y|+z, where x and y are signed integers and z is an unsigned integer.\n+Computes |x-y|+z for signed or unsigned 32-bit integers.\n \n Prototype:\n \n@@ -2988,19 +3122,19 @@ Prototype:\n triton.language.extra.cann.libdevice.sad(x, y, z, _builder=None)\n ```\n \n-Input Types:\n+Supported Signatures:\n \n-- x: `int32`\n-- y: `int32`\n-- z: `int32`\n+- (`int32`, `int32`, `int32`) -> `int32`\n+- (`int32`, `int32`, `uint32`) -> `int32`\n+- (`uint32`, `uint32`, `uint32`) -> `uint32`\n \n Return Value: `tl.tensor`, containing |x-y|+z.\n \n-Return Type: `int32`\n+Return Type: `int32` or `uint32`, as specified by the supported signatures.\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 133. triton.language.extra.cann.libdevice.saturatef\n+## 139. triton.language.extra.cann.libdevice.saturatef\n \n ### OP Overview\n \n@@ -3020,9 +3154,9 @@ Return Value: `tl.tensor`, containing the saturated value of x, in the range \\[+\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 134. triton.language.extra.cann.libdevice.scalbn\n+## 140. triton.language.extra.cann.libdevice.scalbn\n \n ### OP Overview\n \n@@ -3043,9 +3177,9 @@ Return Value: `tl.tensor`, containing the result of x × 2^n.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 135. triton.language.extra.cann.libdevice.signbit\n+## 141. triton.language.extra.cann.libdevice.signbit\n \n ### OP Overview\n \n@@ -3065,9 +3199,9 @@ Return Value: `tl.tensor`, containing the sign bit of x.\n \n Return Type: `int32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 136. triton.language.extra.cann.libdevice.sin\n+## 142. triton.language.extra.cann.libdevice.sin\n \n ### OP Overview\n \n@@ -3087,9 +3221,9 @@ Return Value: `tl.tensor`, containing the sine of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 137. triton.language.extra.cann.libdevice.sinh\n+## 143. triton.language.extra.cann.libdevice.sinh\n \n ### OP Overview\n \n@@ -3109,9 +3243,9 @@ Return Value: `tl.tensor`, containing the hyperbolic sine of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 138. triton.language.extra.cann.libdevice.sinpi\n+## 144. triton.language.extra.cann.libdevice.sinpi\n \n ### OP Overview\n \n@@ -3131,9 +3265,9 @@ Return Value: `tl.tensor`, containing the value of sin(π × x).\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 139. triton.language.extra.cann.libdevice.sqrt\n+## 145. triton.language.extra.cann.libdevice.sqrt\n \n ### OP Overview\n \n@@ -3153,9 +3287,9 @@ Return Value: `tl.tensor`, containing the square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 140. triton.language.extra.cann.libdevice.sqrt_rd\n+## 146. triton.language.extra.cann.libdevice.sqrt_rd\n \n ### OP Overview\n \n@@ -3175,9 +3309,9 @@ Return Value: `tl.tensor`, containing the square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 141. triton.language.extra.cann.libdevice.sqrt_rn\n+## 147. triton.language.extra.cann.libdevice.sqrt_rn\n \n ### OP Overview\n \n@@ -3197,9 +3331,9 @@ Return Value: `tl.tensor`, containing the square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 142. triton.language.extra.cann.libdevice.sqrt_ru\n+## 148. triton.language.extra.cann.libdevice.sqrt_ru\n \n ### OP Overview\n \n@@ -3219,9 +3353,9 @@ Return Value: `tl.tensor`, containing the square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 143. triton.language.extra.cann.libdevice.sqrt_rz\n+## 149. triton.language.extra.cann.libdevice.sqrt_rz\n \n ### OP Overview\n \n@@ -3241,9 +3375,9 @@ Return Value: `tl.tensor`, containing the square root of x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 144. triton.language.extra.cann.libdevice.sub_rd\n+## 150. triton.language.extra.cann.libdevice.sub_rd\n \n ### OP Overview\n \n@@ -3264,9 +3398,9 @@ Return Value: `tl.tensor`, containing the subtraction result rounded down.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 145. triton.language.extra.cann.libdevice.sub_rn\n+## 151. triton.language.extra.cann.libdevice.sub_rn\n \n ### OP Overview\n \n@@ -3287,9 +3421,9 @@ Return Value: `tl.tensor`, containing the subtraction result rounded to the near\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 146. triton.language.extra.cann.libdevice.sub_ru\n+## 152. triton.language.extra.cann.libdevice.sub_ru\n \n ### OP Overview\n \n@@ -3310,9 +3444,9 @@ Return Value: `tl.tensor`, containing the subtraction result rounded up.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 147. triton.language.extra.cann.libdevice.sub_rz\n+## 153. triton.language.extra.cann.libdevice.sub_rz\n \n ### OP Overview\n \n@@ -3333,9 +3467,9 @@ Return Value: `tl.tensor`, containing the subtraction result rounded toward zero\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 148. triton.language.extra.cann.libdevice.tan\n+## 154. triton.language.extra.cann.libdevice.tan\n \n ### OP Overview\n \n@@ -3355,9 +3489,9 @@ Return Value: `tl.tensor`, containing the tangent of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 149. triton.language.extra.cann.libdevice.tanh\n+## 155. triton.language.extra.cann.libdevice.tanh\n \n ### OP Overview\n \n@@ -3377,9 +3511,9 @@ Return Value: `tl.tensor`, containing the hyperbolic tangent of input x.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 150. triton.language.extra.cann.libdevice.tgamma\n+## 156. triton.language.extra.cann.libdevice.tgamma\n \n ### OP Overview\n \n@@ -3399,9 +3533,9 @@ Return Value: `tl.tensor`, containing the gamma function of the input parameter.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 151. triton.language.extra.cann.libdevice.trunc\n+## 157. triton.language.extra.cann.libdevice.trunc\n \n ### OP Overview\n \n@@ -3421,40 +3555,40 @@ Return Value: `tl.tensor`, containing the truncation result.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT, SIMD\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 152. triton.language.extra.cann.libdevice.uint2float_rd\n+## 158. triton.language.extra.cann.libdevice.uint_as_float\n \n ### OP Overview\n \n-Converts a 32-bit unsigned integer to a floating-point number with round-down mode.\n+Reinterprets the bit pattern of a 32-bit unsigned integer as a floating-point number. No numeric conversion is performed.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_rd(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint_as_float(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `uint32`\n \n-Return Value: `tl.tensor`, containing the converted floating-point number.\n+Return Value: `tl.tensor`, containing the bit pattern of the 32-bit unsigned integer reinterpreted as a floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 153. triton.language.extra.cann.libdevice.uint2float_rn\n+## 159. triton.language.extra.cann.libdevice.uint2float_rd\n \n ### OP Overview\n \n-Converts a 32-bit unsigned integer to a floating-point number with round-to-nearest-even mode.\n+Converts a 32-bit unsigned integer to a floating-point number with round-down mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_rn(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_rd(x, _builder=None)\n ```\n \n Input Types:\n@@ -3465,18 +3599,18 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 154. triton.language.extra.cann.libdevice.uint2float_ru\n+## 160. triton.language.extra.cann.libdevice.uint2float_rn\n \n ### OP Overview\n \n-Converts a 32-bit unsigned integer to a floating-point number with round-up mode.\n+Converts a 32-bit unsigned integer to a floating-point number with round-to-nearest-even mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_ru(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_rn(x, _builder=None)\n ```\n \n Input Types:\n@@ -3487,18 +3621,18 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 155. triton.language.extra.cann.libdevice.uint2float_rz\n+## 161. triton.language.extra.cann.libdevice.uint2float_ru\n \n ### OP Overview\n \n-Converts a 32-bit unsigned integer to a floating-point number with round-toward-zero mode.\n+Converts a 32-bit unsigned integer to a floating-point number with round-up mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_rz(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_ru(x, _builder=None)\n ```\n \n Input Types:\n@@ -3509,31 +3643,31 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 156. triton.language.extra.cann.libdevice.uint_as_float\n+## 162. triton.language.extra.cann.libdevice.uint2float_rz\n \n ### OP Overview\n \n-Reinterprets the bit pattern of a 32-bit unsigned integer as a floating-point number. No numeric conversion is performed.\n+Converts a 32-bit unsigned integer to a floating-point number with round-toward-zero mode.\n \n Prototype:\n \n ```python\n-triton.language.extra.cann.libdevice.uint_as_float(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_rz(x, _builder=None)\n ```\n \n Input Types:\n \n - x: `uint32`\n \n-Return Value: `tl.tensor`, containing the bit pattern of the 32-bit unsigned integer reinterpreted as a floating-point number.\n+Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 157. triton.language.extra.cann.libdevice.ull2float_rd\n+## 163. triton.language.extra.cann.libdevice.ull2float_rd\n \n ### OP Overview\n \n@@ -3553,9 +3687,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 158. triton.language.extra.cann.libdevice.ull2float_rn\n+## 164. triton.language.extra.cann.libdevice.ull2float_rn\n \n ### OP Overview\n \n@@ -3575,9 +3709,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 159. triton.language.extra.cann.libdevice.ull2float_ru\n+## 165. triton.language.extra.cann.libdevice.ull2float_ru\n \n ### OP Overview\n \n@@ -3597,9 +3731,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 160. triton.language.extra.cann.libdevice.ull2float_rz\n+## 166. triton.language.extra.cann.libdevice.ull2float_rz\n \n ### OP Overview\n \n@@ -3619,9 +3753,9 @@ Return Value: `tl.tensor`, containing the converted floating-point number.\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 161. triton.language.extra.cann.libdevice.y0\n+## 167. triton.language.extra.cann.libdevice.y0\n \n ### OP Overview\n \n@@ -3641,9 +3775,9 @@ Return Value: `tl.tensor`, containing the Bessel function of the second kind of\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 162. triton.language.extra.cann.libdevice.y1\n+## 168. triton.language.extra.cann.libdevice.y1\n \n ### OP Overview\n \n@@ -3663,9 +3797,9 @@ Return Value: `tl.tensor`, containing the Bessel function of the second kind of\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT\n \n-## 163. triton.language.extra.cann.libdevice.yn\n+## 169. triton.language.extra.cann.libdevice.yn\n \n ### OP Overview\n \n@@ -3686,4 +3820,4 @@ Return Value: `tl.tensor`, containing the Bessel function of the second kind of\n \n Return Type: `float32`\n \n-Supported Compilation Mode: SIMT\n+Supported Compilation Mode: SIMD, SIMT"
        },
        {
          "filename": "docs/zh/libdevice/libdevice_developer_guide.md",
          "status": "modified",
          "additions": 522,
          "deletions": 388,
          "patch": "@@ -2,7 +2,7 @@\n \n ## SIMT 编译示例\n \n-使用 SIMT 编译的 triton kernel 示例\n+使用 SIMT 编译的 triton kernel 示例。通过 kernel 启动参数设置 `compile_mode='simt_only'`，即可启用纯 SIMT 编译。\n \n ```python\n # Enable libdevice SIMT compilation\n@@ -25,8 +25,8 @@ def triton_kernel(input, output, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr)\n dtype, shape, ncore, xblock, xblock_sub = ['int32', (128, 4096), 512, 1024, 1024]\n input = torch.randn(shape, dtype=eval('torch.' + dtype)).npu()\n output = torch.zeros_like(input)\n-# Enable SIMT compilation with option \"force_simt_only=True\"\n-triton_kernel[ncore, 1, 1](input, output, xblock, xblock_sub, force_simt_only=True)\n+# 使用 compile_mode='simt_only' 启用纯 SIMT 编译\n+triton_kernel[ncore, 1, 1](input, output, xblock, xblock_sub, compile_mode='simt_only')\n ```\n \n ## 1. triton.language.extra.cann.libdevice.abs\n@@ -49,7 +49,7 @@ triton.language.extra.cann.libdevice.abs(x, _builder=None)\n \n 返回类型：`int32`, `float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 2. triton.language.extra.cann.libdevice.acos\n \n@@ -71,7 +71,7 @@ triton.language.extra.cann.libdevice.acos(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 3. triton.language.extra.cann.libdevice.acosh\n \n@@ -93,7 +93,7 @@ triton.language.extra.cann.libdevice.acosh(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 4. triton.language.extra.cann.libdevice.add_rd\n \n@@ -116,7 +116,7 @@ triton.language.extra.cann.libdevice.add_rd(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 5. triton.language.extra.cann.libdevice.add_rn\n \n@@ -139,7 +139,7 @@ triton.language.extra.cann.libdevice.add_rn(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 6. triton.language.extra.cann.libdevice.add_ru\n \n@@ -162,7 +162,7 @@ triton.language.extra.cann.libdevice.add_ru(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 7. triton.language.extra.cann.libdevice.add_rz\n \n@@ -185,7 +185,7 @@ triton.language.extra.cann.libdevice.add_rz(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 8. triton.language.extra.cann.libdevice.asin\n \n@@ -207,7 +207,7 @@ triton.language.extra.cann.libdevice.asin(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 9. triton.language.extra.cann.libdevice.asinh\n \n@@ -229,7 +229,7 @@ triton.language.extra.cann.libdevice.asinh(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 10. triton.language.extra.cann.libdevice.atan\n \n@@ -251,7 +251,7 @@ triton.language.extra.cann.libdevice.atan(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 11. triton.language.extra.cann.libdevice.atan2\n \n@@ -274,7 +274,7 @@ triton.language.extra.cann.libdevice.atan2(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 12. triton.language.extra.cann.libdevice.atanh\n \n@@ -296,7 +296,7 @@ triton.language.extra.cann.libdevice.atanh(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 13. triton.language.extra.cann.libdevice.brev\n \n@@ -318,7 +318,7 @@ triton.language.extra.cann.libdevice.brev(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 14. triton.language.extra.cann.libdevice.byte_perm\n \n@@ -356,7 +356,7 @@ triton.language.extra.cann.libdevice.byte_perm(x, y, s, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 15. triton.language.extra.cann.libdevice.cbrt\n \n@@ -378,7 +378,7 @@ triton.language.extra.cann.libdevice.cbrt(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 16. triton.language.extra.cann.libdevice.ceil\n \n@@ -400,7 +400,7 @@ triton.language.extra.cann.libdevice.ceil(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 17. triton.language.extra.cann.libdevice.clz\n \n@@ -422,7 +422,7 @@ triton.language.extra.cann.libdevice.clz(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 18. triton.language.extra.cann.libdevice.copysign\n \n@@ -445,7 +445,7 @@ triton.language.extra.cann.libdevice.copysign(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 19. triton.language.extra.cann.libdevice.cos\n \n@@ -467,7 +467,7 @@ triton.language.extra.cann.libdevice.cos(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 20. triton.language.extra.cann.libdevice.cosh\n \n@@ -489,7 +489,7 @@ triton.language.extra.cann.libdevice.cosh(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 21. triton.language.extra.cann.libdevice.cospi\n \n@@ -511,7 +511,7 @@ triton.language.extra.cann.libdevice.cospi(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 22. triton.language.extra.cann.libdevice.cyl_bessel_i0\n \n@@ -533,7 +533,7 @@ triton.language.extra.cann.libdevice.cyl_bessel_i0(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 23. triton.language.extra.cann.libdevice.cyl_bessel_i1\n \n@@ -555,7 +555,7 @@ triton.language.extra.cann.libdevice.cyl_bessel_i1(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 24. triton.language.extra.cann.libdevice.div_rd\n \n@@ -578,7 +578,7 @@ triton.language.extra.cann.libdevice.div_rd(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 25. triton.language.extra.cann.libdevice.div_rn\n \n@@ -601,7 +601,7 @@ triton.language.extra.cann.libdevice.div_rn(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 26. triton.language.extra.cann.libdevice.div_ru\n \n@@ -624,7 +624,7 @@ triton.language.extra.cann.libdevice.div_ru(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 27. triton.language.extra.cann.libdevice.div_rz\n \n@@ -647,7 +647,7 @@ triton.language.extra.cann.libdevice.div_rz(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 28. triton.language.extra.cann.libdevice.erf\n \n@@ -669,7 +669,7 @@ triton.language.extra.cann.libdevice.erf(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 29. triton.language.extra.cann.libdevice.erfc\n \n@@ -691,7 +691,7 @@ triton.language.extra.cann.libdevice.erfc(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 30. triton.language.extra.cann.libdevice.erfcinv\n \n@@ -713,7 +713,7 @@ triton.language.extra.cann.libdevice.erfcinv(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 31. triton.language.extra.cann.libdevice.erfcx\n \n@@ -735,7 +735,7 @@ triton.language.extra.cann.libdevice.erfcx(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 32. triton.language.extra.cann.libdevice.erfinv\n \n@@ -757,7 +757,7 @@ triton.language.extra.cann.libdevice.erfinv(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 33. triton.language.extra.cann.libdevice.exp\n \n@@ -779,7 +779,7 @@ triton.language.extra.cann.libdevice.exp(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 34. triton.language.extra.cann.libdevice.exp10\n \n@@ -801,7 +801,7 @@ triton.language.extra.cann.libdevice.exp10(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 35. triton.language.extra.cann.libdevice.exp2\n \n@@ -823,7 +823,7 @@ triton.language.extra.cann.libdevice.exp2(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 36. triton.language.extra.cann.libdevice.expm1\n \n@@ -845,7 +845,7 @@ triton.language.extra.cann.libdevice.expm1(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n ## 37. triton.language.extra.cann.libdevice.fast_cosf\n \n@@ -867,7 +867,7 @@ triton.language.extra.cann.libdevice.fast_cosf(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 38. triton.language.extra.cann.libdevice.fast_dividef\n \n@@ -890,117 +890,117 @@ triton.language.extra.cann.libdevice.fast_dividef(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 39. triton.language.extra.cann.libdevice.fast_exp10f\n+## 39. triton.language.extra.cann.libdevice.fast_expf\n \n ### OP概述\n \n-快速近似以 10 为底的指数函数。\n+快速近似指数函数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_exp10f(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_expf(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `float32`\n \n-返回值: `tl.tensor`, 返回快速近似以 10 为底的指数函数的结果。\n+返回值: `tl.tensor`, 返回快速近似指数函数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 40. triton.language.extra.cann.libdevice.fast_expf\n+## 40. triton.language.extra.cann.libdevice.fast_exp10f\n \n ### OP概述\n \n-快速近似指数函数。\n+快速近似以 10 为底的指数函数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_expf(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_exp10f(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `float32`\n \n-返回值: `tl.tensor`, 返回快速近似指数函数的结果。\n+返回值: `tl.tensor`, 返回快速近似以 10 为底的指数函数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 41. triton.language.extra.cann.libdevice.fast_log10f\n+## 41. triton.language.extra.cann.libdevice.fast_logf\n \n ### OP概述\n \n-快速近似以 10 为底的对数函数。\n+快速近似自然对数函数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_log10f(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_logf(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `float32`\n \n-返回值: `tl.tensor`, 返回快速近似以 10 为底的对数函数的结果。\n+返回值: `tl.tensor`, 返回快速近似自然对数函数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 42. triton.language.extra.cann.libdevice.fast_log2f\n+## 42. triton.language.extra.cann.libdevice.fast_log10f\n \n ### OP概述\n \n-快速近似以 2 为底的对数函数。\n+快速近似以 10 为底的对数函数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_log2f(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_log10f(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `float32`\n \n-返回值: `tl.tensor`, 返回快速近似以 2 为底的对数函数的结果。\n+返回值: `tl.tensor`, 返回快速近似以 10 为底的对数函数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 43. triton.language.extra.cann.libdevice.fast_logf\n+## 43. triton.language.extra.cann.libdevice.fast_log2f\n \n ### OP概述\n \n-快速近似自然对数函数。\n+快速近似以 2 为底的对数函数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.fast_logf(x, _builder=None)\n+triton.language.extra.cann.libdevice.fast_log2f(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `float32`\n \n-返回值: `tl.tensor`, 返回快速近似自然对数函数的结果。\n+返回值: `tl.tensor`, 返回快速近似以 2 为底的对数函数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 44. triton.language.extra.cann.libdevice.fast_powf\n \n@@ -1023,7 +1023,7 @@ triton.language.extra.cann.libdevice.fast_powf(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 45. triton.language.extra.cann.libdevice.fast_sinf\n \n@@ -1045,7 +1045,7 @@ triton.language.extra.cann.libdevice.fast_sinf(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n ## 46. triton.language.extra.cann.libdevice.fast_tanf\n \n@@ -1067,9 +1067,31 @@ triton.language.extra.cann.libdevice.fast_tanf(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n+\n+## 47. triton.language.extra.cann.libdevice.fast_tanhf\n+\n+### OP概述\n+\n+使用快速近似算法计算 x 的双曲正切值。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.fast_tanhf(x, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `float32`\n+\n+返回值: `tl.tensor`, 返回 x 的双曲正切快速近似值。\n+\n+返回类型：`float32`\n+\n+支持编译模式：SIMD, SIMT\n \n-## 47. triton.language.extra.cann.libdevice.fdim\n+## 48. triton.language.extra.cann.libdevice.fdim\n \n ### OP概述\n \n@@ -1090,9 +1112,9 @@ triton.language.extra.cann.libdevice.fdim(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 48. triton.language.extra.cann.libdevice.ffs\n+## 49. triton.language.extra.cann.libdevice.ffs\n \n ### OP概述\n \n@@ -1112,9 +1134,9 @@ triton.language.extra.cann.libdevice.ffs(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 49. triton.language.extra.cann.libdevice.finitef\n+## 50. triton.language.extra.cann.libdevice.finitef\n \n ### OP概述\n \n@@ -1134,9 +1156,75 @@ triton.language.extra.cann.libdevice.finitef(x, _builder=None)\n \n 返回类型：`bool`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 50. triton.language.extra.cann.libdevice.float2int_rd\n+## 51. triton.language.extra.cann.libdevice.float_as_int\n+\n+### OP概述\n+\n+将浮点数的比特位重新解释为32位整数。不进行数值转换。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.float_as_int(x, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `float32`\n+\n+返回值: `tl.tensor`, 返回将浮点数的比特位重新解释为32位整数的结果。\n+\n+返回类型：`int32`\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 52. triton.language.extra.cann.libdevice.float_as_uint\n+\n+### OP概述\n+\n+将浮点数的比特位重新解释为32位无符号整数。不进行数值转换。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.float_as_uint(x, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `float32`\n+\n+返回值: `tl.tensor`, 返回将浮点数的比特位重新解释为32位无符号整数的结果。\n+\n+返回类型：`uint32`\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 53. triton.language.extra.cann.libdevice.float2half_rn\n+\n+### OP概述\n+\n+使用最近偶数舍入模式将 x 从 32 位浮点数转换为 16 位浮点数。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.float2half_rn(x, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `float32`\n+\n+返回值: `tl.tensor`, 返回转换后的 16 位浮点数。\n+\n+返回类型：`float16`\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 54. triton.language.extra.cann.libdevice.float2int_rd\n \n ### OP概述\n \n@@ -1156,9 +1244,9 @@ triton.language.extra.cann.libdevice.float2int_rd(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 51. triton.language.extra.cann.libdevice.float2int_rn\n+## 55. triton.language.extra.cann.libdevice.float2int_rn\n \n ### OP概述\n \n@@ -1178,9 +1266,9 @@ triton.language.extra.cann.libdevice.float2int_rn(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 52. triton.language.extra.cann.libdevice.float2int_ru\n+## 56. triton.language.extra.cann.libdevice.float2int_ru\n \n ### OP概述\n \n@@ -1200,9 +1288,9 @@ triton.language.extra.cann.libdevice.float2int_ru(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 53. triton.language.extra.cann.libdevice.float2int_rz\n+## 57. triton.language.extra.cann.libdevice.float2int_rz\n \n ### OP概述\n \n@@ -1222,9 +1310,9 @@ triton.language.extra.cann.libdevice.float2int_rz(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 54. triton.language.extra.cann.libdevice.float2ll_rd\n+## 58. triton.language.extra.cann.libdevice.float2ll_rd\n \n ### OP概述\n \n@@ -1244,9 +1332,9 @@ triton.language.extra.cann.libdevice.float2ll_rd(x, _builder=None)\n \n 返回类型：`int64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 55. triton.language.extra.cann.libdevice.float2ll_rn\n+## 59. triton.language.extra.cann.libdevice.float2ll_rn\n \n ### OP概述\n \n@@ -1266,9 +1354,9 @@ triton.language.extra.cann.libdevice.float2ll_rn(x, _builder=None)\n \n 返回类型：`int64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 56. triton.language.extra.cann.libdevice.float2ll_ru\n+## 60. triton.language.extra.cann.libdevice.float2ll_ru\n \n ### OP概述\n \n@@ -1288,9 +1376,9 @@ triton.language.extra.cann.libdevice.float2ll_ru(x, _builder=None)\n \n 返回类型：`int64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 57. triton.language.extra.cann.libdevice.float2ll_rz\n+## 61. triton.language.extra.cann.libdevice.float2ll_rz\n \n ### OP概述\n \n@@ -1310,9 +1398,9 @@ triton.language.extra.cann.libdevice.float2ll_rz(x, _builder=None)\n \n 返回类型：`int64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 58. triton.language.extra.cann.libdevice.float2uint_rd\n+## 62. triton.language.extra.cann.libdevice.float2uint_rd\n \n ### OP概述\n \n@@ -1332,9 +1420,9 @@ triton.language.extra.cann.libdevice.float2uint_rd(x, _builder=None)\n \n 返回类型：`uint32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 59. triton.language.extra.cann.libdevice.float2uint_rn\n+## 63. triton.language.extra.cann.libdevice.float2uint_rn\n \n ### OP概述\n \n@@ -1354,9 +1442,9 @@ triton.language.extra.cann.libdevice.float2uint_rn(x, _builder=None)\n \n 返回类型：`uint32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 60. triton.language.extra.cann.libdevice.float2uint_ru\n+## 64. triton.language.extra.cann.libdevice.float2uint_ru\n \n ### OP概述\n \n@@ -1376,9 +1464,9 @@ triton.language.extra.cann.libdevice.float2uint_ru(x, _builder=None)\n \n 返回类型：`uint32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 61. triton.language.extra.cann.libdevice.float2uint_rz\n+## 65. triton.language.extra.cann.libdevice.float2uint_rz\n \n ### OP概述\n \n@@ -1398,9 +1486,9 @@ triton.language.extra.cann.libdevice.float2uint_rz(x, _builder=None)\n \n 返回类型：`uint32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 62. triton.language.extra.cann.libdevice.float2ull_rd\n+## 66. triton.language.extra.cann.libdevice.float2ull_rd\n \n ### OP概述\n \n@@ -1420,9 +1508,9 @@ triton.language.extra.cann.libdevice.float2ull_rd(x, _builder=None)\n \n 返回类型：`uint64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 63. triton.language.extra.cann.libdevice.float2ull_rn\n+## 67. triton.language.extra.cann.libdevice.float2ull_rn\n \n ### OP概述\n \n@@ -1442,9 +1530,9 @@ triton.language.extra.cann.libdevice.float2ull_rn(x, _builder=None)\n \n 返回类型：`uint64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 64. triton.language.extra.cann.libdevice.float2ull_ru\n+## 68. triton.language.extra.cann.libdevice.float2ull_ru\n \n ### OP概述\n \n@@ -1464,9 +1552,9 @@ triton.language.extra.cann.libdevice.float2ull_ru(x, _builder=None)\n \n 返回类型：`uint64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 65. triton.language.extra.cann.libdevice.float2ull_rz\n+## 69. triton.language.extra.cann.libdevice.float2ull_rz\n \n ### OP概述\n \n@@ -1486,53 +1574,9 @@ triton.language.extra.cann.libdevice.float2ull_rz(x, _builder=None)\n \n 返回类型：`uint64`\n \n-支持编译模式：SIMT\n-\n-## 66. triton.language.extra.cann.libdevice.float_as_int\n-\n-### OP概述\n-\n-将浮点数的比特位重新解释为32位整数。不进行数值转换。\n-\n-原型:\n-\n-```python\n-triton.language.extra.cann.libdevice.float_as_int(x, _builder=None)\n-```\n-\n-输入类型：\n-\n-- x: `float32`\n-\n-返回值: `tl.tensor`, 返回将浮点数的比特位重新解释为32位整数的结果。\n-\n-返回类型：`int32`\n-\n-支持编译模式：SIMT, SIMD\n-\n-## 67. triton.language.extra.cann.libdevice.float_as_uint\n-\n-### OP概述\n-\n-将浮点数的比特位重新解释为32位无符号整数。不进行数值转换。\n-\n-原型:\n-\n-```python\n-triton.language.extra.cann.libdevice.float_as_uint(x, _builder=None)\n-```\n-\n-输入类型：\n-\n-- x: `float32`\n-\n-返回值: `tl.tensor`, 返回将浮点数的比特位重新解释为32位无符号整数的结果。\n-\n-返回类型：`uint32`\n+支持编译模式：SIMD, SIMT\n \n-支持编译模式：SIMT\n-\n-## 68. triton.language.extra.cann.libdevice.floor\n+## 70. triton.language.extra.cann.libdevice.floor\n \n ### OP概述\n \n@@ -1552,9 +1596,9 @@ triton.language.extra.cann.libdevice.floor(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 69. triton.language.extra.cann.libdevice.fma\n+## 71. triton.language.extra.cann.libdevice.fma\n \n ### OP概述\n \n@@ -1576,9 +1620,9 @@ triton.language.extra.cann.libdevice.fma(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 70. triton.language.extra.cann.libdevice.fma_rd\n+## 72. triton.language.extra.cann.libdevice.fma_rd\n \n ### OP概述\n \n@@ -1600,9 +1644,9 @@ triton.language.extra.cann.libdevice.fma_rd(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 71. triton.language.extra.cann.libdevice.fma_rn\n+## 73. triton.language.extra.cann.libdevice.fma_rn\n \n ### OP概述\n \n@@ -1624,9 +1668,9 @@ triton.language.extra.cann.libdevice.fma_rn(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 72. triton.language.extra.cann.libdevice.fma_ru\n+## 74. triton.language.extra.cann.libdevice.fma_ru\n \n ### OP概述\n \n@@ -1648,9 +1692,9 @@ triton.language.extra.cann.libdevice.fma_ru(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 73. triton.language.extra.cann.libdevice.fma_rz\n+## 75. triton.language.extra.cann.libdevice.fma_rz\n \n ### OP概述\n \n@@ -1672,9 +1716,9 @@ triton.language.extra.cann.libdevice.fma_rz(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 74. triton.language.extra.cann.libdevice.fmod\n+## 76. triton.language.extra.cann.libdevice.fmod\n \n ### OP概述\n \n@@ -1695,9 +1739,9 @@ triton.language.extra.cann.libdevice.fmod(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 75. triton.language.extra.cann.libdevice.gamma\n+## 77. triton.language.extra.cann.libdevice.gamma\n \n ### OP概述\n \n@@ -1717,9 +1761,9 @@ triton.language.extra.cann.libdevice.gamma(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 76. triton.language.extra.cann.libdevice.hadd\n+## 78. triton.language.extra.cann.libdevice.hadd\n \n ### OP概述\n \n@@ -1733,16 +1777,38 @@ triton.language.extra.cann.libdevice.hadd(x, y, _builder=None)\n \n 输入类型：\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32` 或 `uint32`\n+- y: `int32` 或 `uint32`，类型必须与 x 相同\n \n 返回值: `tl.tensor`, 返回 x 和 y 的平均值。\n \n-返回类型：`int32`\n+返回类型：与输入类型相同（`int32` 或 `uint32`）。\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 77. triton.language.extra.cann.libdevice.hypot\n+## 79. triton.language.extra.cann.libdevice.half2float\n+\n+### OP概述\n+\n+将 x 从 16 位浮点数转换为 32 位浮点数。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.half2float(x, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `float16`\n+\n+返回值: `tl.tensor`, 返回转换后的 32 位浮点数。\n+\n+返回类型：`float32`\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 80. triton.language.extra.cann.libdevice.hypot\n \n ### OP概述\n \n@@ -1763,9 +1829,9 @@ triton.language.extra.cann.libdevice.hypot(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 78. triton.language.extra.cann.libdevice.ilogb\n+## 81. triton.language.extra.cann.libdevice.ilogb\n \n ### OP概述\n \n@@ -1785,40 +1851,40 @@ triton.language.extra.cann.libdevice.ilogb(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 79. triton.language.extra.cann.libdevice.int2float_rd\n+## 82. triton.language.extra.cann.libdevice.int_as_float\n \n ### OP概述\n \n-向下舍入模式将32位整数转换为浮点数。\n+将32位整数的比特位重新解释为浮点数。不进行数值转换。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_rd(x, _builder=None)\n+triton.language.extra.cann.libdevice.int_as_float(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `int32`\n \n-返回值: `tl.tensor`, 返回转换后的浮点数。\n+返回值: `tl.tensor`, 返回将32位整数的比特位重新解释为浮点数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 80. triton.language.extra.cann.libdevice.int2float_rn\n+## 83. triton.language.extra.cann.libdevice.int2float_rd\n \n ### OP概述\n \n-最近偶数舍入模式将32位整数转换为浮点数。\n+向下舍入模式将32位整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_rn(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_rd(x, _builder=None)\n ```\n \n 输入类型：\n@@ -1829,18 +1895,18 @@ triton.language.extra.cann.libdevice.int2float_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 81. triton.language.extra.cann.libdevice.int2float_ru\n+## 84. triton.language.extra.cann.libdevice.int2float_rn\n \n ### OP概述\n \n-向上舍入模式将32位整数转换为浮点数。\n+最近偶数舍入模式将32位整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_ru(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_rn(x, _builder=None)\n ```\n \n 输入类型：\n@@ -1851,18 +1917,18 @@ triton.language.extra.cann.libdevice.int2float_ru(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 82. triton.language.extra.cann.libdevice.int2float_rz\n+## 85. triton.language.extra.cann.libdevice.int2float_ru\n \n ### OP概述\n \n-向零舍入模式将32位整数转换为浮点数。\n+向上舍入模式将32位整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.int2float_rz(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_ru(x, _builder=None)\n ```\n \n 输入类型：\n@@ -1873,31 +1939,31 @@ triton.language.extra.cann.libdevice.int2float_rz(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 83. triton.language.extra.cann.libdevice.int_as_float\n+## 86. triton.language.extra.cann.libdevice.int2float_rz\n \n ### OP概述\n \n-将32位整数的比特位重新解释为浮点数。不进行数值转换。\n+向零舍入模式将32位整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.int_as_float(x, _builder=None)\n+triton.language.extra.cann.libdevice.int2float_rz(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `int32`\n \n-返回值: `tl.tensor`, 返回将32位整数的比特位重新解释为浮点数的结果。\n+返回值: `tl.tensor`, 返回转换后的浮点数。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 84. triton.language.extra.cann.libdevice.isinf\n+## 87. triton.language.extra.cann.libdevice.isinf\n \n ### OP概述\n \n@@ -1917,9 +1983,9 @@ triton.language.extra.cann.libdevice.isinf(x, _builder=None)\n \n 返回类型：`bool`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 85. triton.language.extra.cann.libdevice.isnan\n+## 88. triton.language.extra.cann.libdevice.isnan\n \n ### OP概述\n \n@@ -1939,9 +2005,9 @@ triton.language.extra.cann.libdevice.isnan(x, _builder=None)\n \n 返回类型：`bool`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 86. triton.language.extra.cann.libdevice.j0\n+## 89. triton.language.extra.cann.libdevice.j0\n \n ### OP概述\n \n@@ -1961,9 +2027,9 @@ triton.language.extra.cann.libdevice.j0(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 87. triton.language.extra.cann.libdevice.j1\n+## 90. triton.language.extra.cann.libdevice.j1\n \n ### OP概述\n \n@@ -1983,9 +2049,9 @@ triton.language.extra.cann.libdevice.j1(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 88. triton.language.extra.cann.libdevice.jn\n+## 91. triton.language.extra.cann.libdevice.jn\n \n ### OP概述\n \n@@ -2006,9 +2072,9 @@ triton.language.extra.cann.libdevice.jn(n, x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 89. triton.language.extra.cann.libdevice.ldexp\n+## 92. triton.language.extra.cann.libdevice.ldexp\n \n ### OP概述\n \n@@ -2029,9 +2095,9 @@ triton.language.extra.cann.libdevice.ldexp(x, exp, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 90. triton.language.extra.cann.libdevice.lgamma\n+## 93. triton.language.extra.cann.libdevice.lgamma\n \n ### OP概述\n \n@@ -2051,9 +2117,9 @@ triton.language.extra.cann.libdevice.lgamma(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 91. triton.language.extra.cann.libdevice.ll2float_rd\n+## 94. triton.language.extra.cann.libdevice.ll2float_rd\n \n ### OP概述\n \n@@ -2073,9 +2139,9 @@ triton.language.extra.cann.libdevice.ll2float_rd(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 92. triton.language.extra.cann.libdevice.ll2float_rn\n+## 95. triton.language.extra.cann.libdevice.ll2float_rn\n \n ### OP概述\n \n@@ -2095,9 +2161,9 @@ triton.language.extra.cann.libdevice.ll2float_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 93. triton.language.extra.cann.libdevice.ll2float_ru\n+## 96. triton.language.extra.cann.libdevice.ll2float_ru\n \n ### OP概述\n \n@@ -2117,9 +2183,9 @@ triton.language.extra.cann.libdevice.ll2float_ru(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 94. triton.language.extra.cann.libdevice.ll2float_rz\n+## 97. triton.language.extra.cann.libdevice.ll2float_rz\n \n ### OP概述\n \n@@ -2139,9 +2205,9 @@ triton.language.extra.cann.libdevice.ll2float_rz(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 95. triton.language.extra.cann.libdevice.llrint\n+## 98. triton.language.extra.cann.libdevice.llrint\n \n ### OP概述\n \n@@ -2161,9 +2227,9 @@ triton.language.extra.cann.libdevice.llrint(x, _builder=None)\n \n 返回类型：`int64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 96. triton.language.extra.cann.libdevice.llround\n+## 99. triton.language.extra.cann.libdevice.llround\n \n ### OP概述\n \n@@ -2183,9 +2249,9 @@ triton.language.extra.cann.libdevice.llround(x, _builder=None)\n \n 返回类型：`int64`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 97. triton.language.extra.cann.libdevice.log\n+## 100. triton.language.extra.cann.libdevice.log\n \n ### OP概述\n \n@@ -2205,9 +2271,9 @@ triton.language.extra.cann.libdevice.log(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 98. triton.language.extra.cann.libdevice.log10\n+## 101. triton.language.extra.cann.libdevice.log10\n \n ### OP概述\n \n@@ -2227,9 +2293,9 @@ triton.language.extra.cann.libdevice.log10(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 99. triton.language.extra.cann.libdevice.log1p\n+## 102. triton.language.extra.cann.libdevice.log1p\n \n ### OP概述\n \n@@ -2249,9 +2315,9 @@ triton.language.extra.cann.libdevice.log1p(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 100. triton.language.extra.cann.libdevice.log2\n+## 103. triton.language.extra.cann.libdevice.log2\n \n ### OP概述\n \n@@ -2271,9 +2337,9 @@ triton.language.extra.cann.libdevice.log2(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 101. triton.language.extra.cann.libdevice.logb\n+## 104. triton.language.extra.cann.libdevice.logb\n \n ### OP概述\n \n@@ -2293,32 +2359,55 @@ triton.language.extra.cann.libdevice.logb(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 102. triton.language.extra.cann.libdevice.mul24\n+## 105. triton.language.extra.cann.libdevice.max\n \n ### OP概述\n \n-计算 x 和 y 的低24位乘法结果。\n+逐元素计算 x 和 y 的最大值。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.mul24(x, y, _builder=None)\n+triton.language.extra.cann.libdevice.max(x, y, _builder=None)\n ```\n \n 输入类型：\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32`, `float32`\n+- y: `int32`, `float32`，类型必须与 x 相同\n \n-返回值: `tl.tensor`, 返回 x 和 y 的低24位乘法结果。\n+返回值: `tl.tensor`, 返回 x 和 y 的逐元素最大值。\n \n-返回类型：`int32`\n+返回类型：与输入类型相同（`int32` 或 `float32`）。\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 106. triton.language.extra.cann.libdevice.min\n+\n+### OP概述\n+\n+逐元素计算 x 和 y 的最小值。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.min(x, y, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `int32`, `float32`\n+- y: `int32`, `float32`，类型必须与 x 相同\n+\n+返回值: `tl.tensor`, 返回 x 和 y 的逐元素最小值。\n+\n+返回类型：与输入类型相同（`int32` 或 `float32`）。\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 103. triton.language.extra.cann.libdevice.mul_rd\n+## 107. triton.language.extra.cann.libdevice.mul_rd\n \n ### OP概述\n \n@@ -2339,9 +2428,9 @@ triton.language.extra.cann.libdevice.mul_rd(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 104. triton.language.extra.cann.libdevice.mul_rn\n+## 108. triton.language.extra.cann.libdevice.mul_rn\n \n ### OP概述\n \n@@ -2362,9 +2451,9 @@ triton.language.extra.cann.libdevice.mul_rn(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 105. triton.language.extra.cann.libdevice.mul_ru\n+## 109. triton.language.extra.cann.libdevice.mul_ru\n \n ### OP概述\n \n@@ -2385,9 +2474,9 @@ triton.language.extra.cann.libdevice.mul_ru(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 106. triton.language.extra.cann.libdevice.mul_rz\n+## 110. triton.language.extra.cann.libdevice.mul_rz\n \n ### OP概述\n \n@@ -2408,9 +2497,32 @@ triton.language.extra.cann.libdevice.mul_rz(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 107. triton.language.extra.cann.libdevice.mulhi\n+## 111. triton.language.extra.cann.libdevice.mul24\n+\n+### OP概述\n+\n+计算 x 和 y 的低24位乘法结果。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.mul24(x, y, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `int32` 或 `uint32`\n+- y: `int32` 或 `uint32`，类型必须与 x 相同\n+\n+返回值: `tl.tensor`, 返回 x 和 y 的低24位乘法结果。\n+\n+返回类型：与输入类型相同（`int32` 或 `uint32`）。\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 112. triton.language.extra.cann.libdevice.mulhi\n \n ### OP概述\n \n@@ -2424,16 +2536,38 @@ triton.language.extra.cann.libdevice.mulhi(x, y, _builder=None)\n \n 输入类型：\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32` 或 `uint32`\n+- y: `int32` 或 `uint32`，类型必须与 x 相同\n \n 返回值: `tl.tensor`, 返回 x 和 y 的乘法结果的高 32 位。\n \n-返回类型：`int32`\n+返回类型：与输入类型相同（`int32` 或 `uint32`）。\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 108. triton.language.extra.cann.libdevice.nearbyint\n+## 113. triton.language.extra.cann.libdevice.nan\n+\n+### OP概述\n+\n+根据 x 生成 NaN 值。\n+\n+原型:\n+\n+```python\n+triton.language.extra.cann.libdevice.nan(x, _builder=None)\n+```\n+\n+输入类型：\n+\n+- x: `float32`\n+\n+返回值: `tl.tensor`, 返回生成的 NaN 值。\n+\n+返回类型：`float32`\n+\n+支持编译模式：SIMD, SIMT\n+\n+## 114. triton.language.extra.cann.libdevice.nearbyint\n \n ### OP概述\n \n@@ -2453,9 +2587,9 @@ triton.language.extra.cann.libdevice.nearbyint(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 109. triton.language.extra.cann.libdevice.nextafter\n+## 115. triton.language.extra.cann.libdevice.nextafter\n \n ### OP概述\n \n@@ -2476,9 +2610,9 @@ triton.language.extra.cann.libdevice.nextafter(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 110. triton.language.extra.cann.libdevice.norm3d\n+## 116. triton.language.extra.cann.libdevice.norm3d\n \n ### OP概述\n \n@@ -2500,9 +2634,9 @@ triton.language.extra.cann.libdevice.norm3d(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 111. triton.language.extra.cann.libdevice.norm4d\n+## 117. triton.language.extra.cann.libdevice.norm4d\n \n ### OP概述\n \n@@ -2525,9 +2659,9 @@ triton.language.extra.cann.libdevice.norm4d(x, y, z, w, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 112. triton.language.extra.cann.libdevice.normcdf\n+## 118. triton.language.extra.cann.libdevice.normcdf\n \n ### OP概述\n \n@@ -2547,9 +2681,9 @@ triton.language.extra.cann.libdevice.normcdf(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 113. triton.language.extra.cann.libdevice.normcdfinv\n+## 119. triton.language.extra.cann.libdevice.normcdfinv\n \n ### OP概述\n \n@@ -2569,9 +2703,9 @@ triton.language.extra.cann.libdevice.normcdfinv(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 114. triton.language.extra.cann.libdevice.popc\n+## 120. triton.language.extra.cann.libdevice.popc\n \n ### OP概述\n \n@@ -2591,9 +2725,9 @@ triton.language.extra.cann.libdevice.popc(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 115. triton.language.extra.cann.libdevice.pow\n+## 121. triton.language.extra.cann.libdevice.pow\n \n ### OP概述\n \n@@ -2614,9 +2748,9 @@ triton.language.extra.cann.libdevice.pow(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 116. triton.language.extra.cann.libdevice.rcbrt\n+## 122. triton.language.extra.cann.libdevice.rcbrt\n \n ### OP概述\n \n@@ -2636,9 +2770,9 @@ triton.language.extra.cann.libdevice.rcbrt(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 117. triton.language.extra.cann.libdevice.rcp_rd\n+## 123. triton.language.extra.cann.libdevice.rcp_rd\n \n ### OP概述\n \n@@ -2658,9 +2792,9 @@ triton.language.extra.cann.libdevice.rcp_rd(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 118. triton.language.extra.cann.libdevice.rcp_rn\n+## 124. triton.language.extra.cann.libdevice.rcp_rn\n \n ### OP概述\n \n@@ -2680,9 +2814,9 @@ triton.language.extra.cann.libdevice.rcp_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 119. triton.language.extra.cann.libdevice.rcp_ru\n+## 125. triton.language.extra.cann.libdevice.rcp_ru\n \n ### OP概述\n \n@@ -2702,9 +2836,9 @@ triton.language.extra.cann.libdevice.rcp_ru(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 120. triton.language.extra.cann.libdevice.rcp_rz\n+## 126. triton.language.extra.cann.libdevice.rcp_rz\n \n ### OP概述\n \n@@ -2724,9 +2858,9 @@ triton.language.extra.cann.libdevice.rcp_rz(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 121. triton.language.extra.cann.libdevice.reciprocal\n+## 127. triton.language.extra.cann.libdevice.reciprocal\n \n ### OP概述\n \n@@ -2746,9 +2880,9 @@ triton.language.extra.cann.libdevice.reciprocal(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 122. triton.language.extra.cann.libdevice.relu\n+## 128. triton.language.extra.cann.libdevice.relu\n \n ### OP概述\n \n@@ -2768,9 +2902,9 @@ triton.language.extra.cann.libdevice.relu(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 123. triton.language.extra.cann.libdevice.remainder\n+## 129. triton.language.extra.cann.libdevice.remainder\n \n ### OP概述\n \n@@ -2791,9 +2925,9 @@ triton.language.extra.cann.libdevice.remainder(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 124. triton.language.extra.cann.libdevice.rhadd\n+## 130. triton.language.extra.cann.libdevice.rhadd\n \n ### OP概述\n \n@@ -2807,16 +2941,16 @@ triton.language.extra.cann.libdevice.rhadd(x, y, _builder=None)\n \n 输入类型：\n \n-- x: `int32`\n-- y: `int32`\n+- x: `int32` 或 `uint32`\n+- y: `int32` 或 `uint32`，类型必须与 x 相同\n \n 返回值: `tl.tensor`, 返回 x 和 y 平均值的取整结果。\n \n-返回类型：`int32`\n+返回类型：与输入类型相同（`int32` 或 `uint32`）。\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 125. triton.language.extra.cann.libdevice.rhypot\n+## 131. triton.language.extra.cann.libdevice.rhypot\n \n ### OP概述\n \n@@ -2837,9 +2971,9 @@ triton.language.extra.cann.libdevice.rhypot(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 126. triton.language.extra.cann.libdevice.rint\n+## 132. triton.language.extra.cann.libdevice.rint\n \n ### OP概述\n \n@@ -2859,9 +2993,9 @@ triton.language.extra.cann.libdevice.rint(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 127. triton.language.extra.cann.libdevice.rnorm3d\n+## 133. triton.language.extra.cann.libdevice.rnorm3d\n \n ### OP概述\n \n@@ -2883,9 +3017,9 @@ triton.language.extra.cann.libdevice.rnorm3d(x, y, z, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 128. triton.language.extra.cann.libdevice.rnorm4d\n+## 134. triton.language.extra.cann.libdevice.rnorm4d\n \n ### OP概述\n \n@@ -2908,9 +3042,9 @@ triton.language.extra.cann.libdevice.rnorm4d(x, y, z, w, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 129. triton.language.extra.cann.libdevice.round\n+## 135. triton.language.extra.cann.libdevice.round\n \n ### OP概述\n \n@@ -2930,9 +3064,9 @@ triton.language.extra.cann.libdevice.round(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 130. triton.language.extra.cann.libdevice.rsqrt\n+## 136. triton.language.extra.cann.libdevice.rsqrt\n \n ### OP概述\n \n@@ -2952,9 +3086,9 @@ triton.language.extra.cann.libdevice.rsqrt(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 131. triton.language.extra.cann.libdevice.rsqrt_rn\n+## 137. triton.language.extra.cann.libdevice.rsqrt_rn\n \n ### OP概述\n \n@@ -2974,13 +3108,13 @@ triton.language.extra.cann.libdevice.rsqrt_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 132. triton.language.extra.cann.libdevice.sad\n+## 138. triton.language.extra.cann.libdevice.sad\n \n ### OP概述\n \n-计算 |x-y|+z，其中 x 和 y 是有符号整数，z 是无符号整数。\n+计算有符号或无符号 32 位整数的 |x-y|+z。\n \n 原型:\n \n@@ -2988,19 +3122,19 @@ triton.language.extra.cann.libdevice.rsqrt_rn(x, _builder=None)\n triton.language.extra.cann.libdevice.sad(x, y, z, _builder=None)\n ```\n \n-输入类型：\n+支持的类型签名：\n \n-- x: `int32`\n-- y: `int32`\n-- z: `int32`\n+- (`int32`, `int32`, `int32`) -> `int32`\n+- (`int32`, `int32`, `uint32`) -> `int32`\n+- (`uint32`, `uint32`, `uint32`) -> `uint32`\n \n 返回值: `tl.tensor`, 返回 |x-y|+z。\n \n-返回类型：`int32`\n+返回类型：`int32` 或 `uint32`，具体类型由上述类型签名决定。\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 133. triton.language.extra.cann.libdevice.saturatef\n+## 139. triton.language.extra.cann.libdevice.saturatef\n \n ### OP概述\n \n@@ -3020,9 +3154,9 @@ triton.language.extra.cann.libdevice.saturatef(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 134. triton.language.extra.cann.libdevice.scalbn\n+## 140. triton.language.extra.cann.libdevice.scalbn\n \n ### OP概述\n \n@@ -3043,9 +3177,9 @@ triton.language.extra.cann.libdevice.scalbn(x, n, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 135. triton.language.extra.cann.libdevice.signbit\n+## 141. triton.language.extra.cann.libdevice.signbit\n \n ### OP概述\n \n@@ -3065,9 +3199,9 @@ triton.language.extra.cann.libdevice.signbit(x, _builder=None)\n \n 返回类型：`int32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 136. triton.language.extra.cann.libdevice.sin\n+## 142. triton.language.extra.cann.libdevice.sin\n \n ### OP概述\n \n@@ -3087,9 +3221,9 @@ triton.language.extra.cann.libdevice.sin(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 137. triton.language.extra.cann.libdevice.sinh\n+## 143. triton.language.extra.cann.libdevice.sinh\n \n ### OP概述\n \n@@ -3109,9 +3243,9 @@ triton.language.extra.cann.libdevice.sinh(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 138. triton.language.extra.cann.libdevice.sinpi\n+## 144. triton.language.extra.cann.libdevice.sinpi\n \n ### OP概述\n \n@@ -3131,9 +3265,9 @@ triton.language.extra.cann.libdevice.sinpi(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 139. triton.language.extra.cann.libdevice.sqrt\n+## 145. triton.language.extra.cann.libdevice.sqrt\n \n ### OP概述\n \n@@ -3153,9 +3287,9 @@ triton.language.extra.cann.libdevice.sqrt(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 140. triton.language.extra.cann.libdevice.sqrt_rd\n+## 146. triton.language.extra.cann.libdevice.sqrt_rd\n \n ### OP概述\n \n@@ -3175,9 +3309,9 @@ triton.language.extra.cann.libdevice.sqrt_rd(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 141. triton.language.extra.cann.libdevice.sqrt_rn\n+## 147. triton.language.extra.cann.libdevice.sqrt_rn\n \n ### OP概述\n \n@@ -3197,9 +3331,9 @@ triton.language.extra.cann.libdevice.sqrt_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 142. triton.language.extra.cann.libdevice.sqrt_ru\n+## 148. triton.language.extra.cann.libdevice.sqrt_ru\n \n ### OP概述\n \n@@ -3219,9 +3353,9 @@ triton.language.extra.cann.libdevice.sqrt_ru(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 143. triton.language.extra.cann.libdevice.sqrt_rz\n+## 149. triton.language.extra.cann.libdevice.sqrt_rz\n \n ### OP概述\n \n@@ -3241,9 +3375,9 @@ triton.language.extra.cann.libdevice.sqrt_rz(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 144. triton.language.extra.cann.libdevice.sub_rd\n+## 150. triton.language.extra.cann.libdevice.sub_rd\n \n ### OP概述\n \n@@ -3264,9 +3398,9 @@ triton.language.extra.cann.libdevice.sub_rd(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 145. triton.language.extra.cann.libdevice.sub_rn\n+## 151. triton.language.extra.cann.libdevice.sub_rn\n \n ### OP概述\n \n@@ -3287,9 +3421,9 @@ triton.language.extra.cann.libdevice.sub_rn(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 146. triton.language.extra.cann.libdevice.sub_ru\n+## 152. triton.language.extra.cann.libdevice.sub_ru\n \n ### OP概述\n \n@@ -3310,9 +3444,9 @@ triton.language.extra.cann.libdevice.sub_ru(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 147. triton.language.extra.cann.libdevice.sub_rz\n+## 153. triton.language.extra.cann.libdevice.sub_rz\n \n ### OP概述\n \n@@ -3333,9 +3467,9 @@ triton.language.extra.cann.libdevice.sub_rz(x, y, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 148. triton.language.extra.cann.libdevice.tan\n+## 154. triton.language.extra.cann.libdevice.tan\n \n ### OP概述\n \n@@ -3355,9 +3489,9 @@ triton.language.extra.cann.libdevice.tan(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 149. triton.language.extra.cann.libdevice.tanh\n+## 155. triton.language.extra.cann.libdevice.tanh\n \n ### OP概述\n \n@@ -3377,9 +3511,9 @@ triton.language.extra.cann.libdevice.tanh(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 150. triton.language.extra.cann.libdevice.tgamma\n+## 156. triton.language.extra.cann.libdevice.tgamma\n \n ### OP概述\n \n@@ -3399,9 +3533,9 @@ triton.language.extra.cann.libdevice.tgamma(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 151. triton.language.extra.cann.libdevice.trunc\n+## 157. triton.language.extra.cann.libdevice.trunc\n \n ### OP概述\n \n@@ -3421,40 +3555,40 @@ triton.language.extra.cann.libdevice.trunc(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT, SIMD\n+支持编译模式：SIMD, SIMT\n \n-## 152. triton.language.extra.cann.libdevice.uint2float_rd\n+## 158. triton.language.extra.cann.libdevice.uint_as_float\n \n ### OP概述\n \n-向下舍入模式将32位无符号整数转换为浮点数。\n+将32位无符号整数的比特位重新解释为浮点数。不进行数值转换。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_rd(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint_as_float(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `uint32`\n \n-返回值: `tl.tensor`, 返回转换后的浮点数。\n+返回值: `tl.tensor`, 返回将32位无符号整数的比特位重新解释为浮点数的结果。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 153. triton.language.extra.cann.libdevice.uint2float_rn\n+## 159. triton.language.extra.cann.libdevice.uint2float_rd\n \n ### OP概述\n \n-最近偶数舍入模式将32位无符号整数转换为浮点数。\n+向下舍入模式将32位无符号整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_rn(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_rd(x, _builder=None)\n ```\n \n 输入类型：\n@@ -3465,18 +3599,18 @@ triton.language.extra.cann.libdevice.uint2float_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 154. triton.language.extra.cann.libdevice.uint2float_ru\n+## 160. triton.language.extra.cann.libdevice.uint2float_rn\n \n ### OP概述\n \n-向上舍入模式将32位无符号整数转换为浮点数。\n+最近偶数舍入模式将32位无符号整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_ru(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_rn(x, _builder=None)\n ```\n \n 输入类型：\n@@ -3487,18 +3621,18 @@ triton.language.extra.cann.libdevice.uint2float_ru(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 155. triton.language.extra.cann.libdevice.uint2float_rz\n+## 161. triton.language.extra.cann.libdevice.uint2float_ru\n \n ### OP概述\n \n-向零舍入模式将32位无符号整数转换为浮点数。\n+向上舍入模式将32位无符号整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.uint2float_rz(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_ru(x, _builder=None)\n ```\n \n 输入类型：\n@@ -3509,31 +3643,31 @@ triton.language.extra.cann.libdevice.uint2float_rz(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 156. triton.language.extra.cann.libdevice.uint_as_float\n+## 162. triton.language.extra.cann.libdevice.uint2float_rz\n \n ### OP概述\n \n-将32位无符号整数的比特位重新解释为浮点数。不进行数值转换。\n+向零舍入模式将32位无符号整数转换为浮点数。\n \n 原型:\n \n ```python\n-triton.language.extra.cann.libdevice.uint_as_float(x, _builder=None)\n+triton.language.extra.cann.libdevice.uint2float_rz(x, _builder=None)\n ```\n \n 输入类型：\n \n - x: `uint32`\n \n-返回值: `tl.tensor`, 返回将32位无符号整数的比特位重新解释为浮点数的结果。\n+返回值: `tl.tensor`, 返回转换后的浮点数。\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 157. triton.language.extra.cann.libdevice.ull2float_rd\n+## 163. triton.language.extra.cann.libdevice.ull2float_rd\n \n ### OP概述\n \n@@ -3553,9 +3687,9 @@ triton.language.extra.cann.libdevice.ull2float_rd(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 158. triton.language.extra.cann.libdevice.ull2float_rn\n+## 164. triton.language.extra.cann.libdevice.ull2float_rn\n \n ### OP概述\n \n@@ -3575,9 +3709,9 @@ triton.language.extra.cann.libdevice.ull2float_rn(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 159. triton.language.extra.cann.libdevice.ull2float_ru\n+## 165. triton.language.extra.cann.libdevice.ull2float_ru\n \n ### OP概述\n \n@@ -3597,9 +3731,9 @@ triton.language.extra.cann.libdevice.ull2float_ru(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 160. triton.language.extra.cann.libdevice.ull2float_rz\n+## 166. triton.language.extra.cann.libdevice.ull2float_rz\n \n ### OP概述\n \n@@ -3619,9 +3753,9 @@ triton.language.extra.cann.libdevice.ull2float_rz(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 161. triton.language.extra.cann.libdevice.y0\n+## 167. triton.language.extra.cann.libdevice.y0\n \n ### OP概述\n \n@@ -3641,9 +3775,9 @@ triton.language.extra.cann.libdevice.y0(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 162. triton.language.extra.cann.libdevice.y1\n+## 168. triton.language.extra.cann.libdevice.y1\n \n ### OP概述\n \n@@ -3663,9 +3797,9 @@ triton.language.extra.cann.libdevice.y1(x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT\n \n-## 163. triton.language.extra.cann.libdevice.yn\n+## 169. triton.language.extra.cann.libdevice.yn\n \n ### OP概述\n \n@@ -3686,4 +3820,4 @@ triton.language.extra.cann.libdevice.yn(n, x, _builder=None)\n \n 返回类型：`float32`\n \n-支持编译模式：SIMT\n+支持编译模式：SIMD, SIMT"
        },
        {
          "filename": "docs/zh/python-api/_ascend_constraints.py",
          "status": "modified",
          "additions": 80,
          "deletions": 0,
          "patch": "@@ -3082,6 +3082,19 @@ CONSTRAINTS = {\n         \"example\":\n         \"triton.language.extra.cann.libdevice.fast_tanf\",\n     },\n+    \"triton.language.extra.cann.libdevice.fast_tanhf\": {\n+        \"replace_docstring\": [\n+            \"Computes the hyperbolic tangent of x using a fast approximation.\",\n+        ],\n+        \"constraints\": [\n+            \"- x: ``float32``\",\n+            \"Return value: ``tl.tensor``, returns the fast approximate hyperbolic tangent of x.\",\n+            \"Return type: ``float32``\",\n+            \"Compilation modes: SIMT\",\n+        ],\n+        \"example\":\n+        \"triton.language.extra.cann.libdevice.fast_tanhf\",\n+    },\n     \"triton.language.extra.cann.libdevice.fdim\": {\n         \"replace_docstring\": [\n             \"Computes the positive difference max(x - y, 0).\",\n@@ -3122,6 +3135,19 @@ CONSTRAINTS = {\n         \"example\":\n         \"triton.language.extra.cann.libdevice.finitef\",\n     },\n+    \"triton.language.extra.cann.libdevice.float2half_rn\": {\n+        \"replace_docstring\": [\n+            \"Converts x from float32 to float16, rounding to nearest even.\",\n+        ],\n+        \"constraints\": [\n+            \"- x: ``float32``\",\n+            \"Return value: ``tl.tensor``, returns the converted 16-bit floating-point value.\",\n+            \"Return type: ``float16``\",\n+            \"Compilation modes: SIMT\",\n+        ],\n+        \"example\":\n+        \"triton.language.extra.cann.libdevice.float2half_rn\",\n+    },\n     \"triton.language.extra.cann.libdevice.float2int_rd\": {\n         \"replace_docstring\": [\n             \"Converts x to int32, rounding down.\",\n@@ -3476,6 +3502,19 @@ CONSTRAINTS = {\n         \"example\":\n         \"triton.language.extra.cann.libdevice.hadd\",\n     },\n+    \"triton.language.extra.cann.libdevice.half2float\": {\n+        \"replace_docstring\": [\n+            \"Converts x from float16 to float32.\",\n+        ],\n+        \"constraints\": [\n+            \"- x: ``float16``\",\n+            \"Return value: ``tl.tensor``, returns the converted 32-bit floating-point value.\",\n+            \"Return type: ``float32``\",\n+            \"Compilation modes: SIMT\",\n+        ],\n+        \"example\":\n+        \"triton.language.extra.cann.libdevice.half2float\",\n+    },\n     \"triton.language.extra.cann.libdevice.hypot\": {\n         \"constraints\": [\n             \"- x: ``float32``\",\n@@ -3780,6 +3819,34 @@ CONSTRAINTS = {\n         \"example\":\n         \"triton.language.extra.cann.libdevice.logb\",\n     },\n+    \"triton.language.extra.cann.libdevice.max\": {\n+        \"replace_docstring\": [\n+            \"Computes the maximum of x and y.\",\n+        ],\n+        \"constraints\": [\n+            \"- x: ``int32`` or ``float32``\",\n+            \"- y: ``int32`` or ``float32``; must have the same type as x\",\n+            \"Return value: ``tl.tensor``, returns the maximum of x and y.\",\n+            \"Return type: same as the input type\",\n+            \"Compilation modes: SIMT\",\n+        ],\n+        \"example\":\n+        \"triton.language.extra.cann.libdevice.max\",\n+    },\n+    \"triton.language.extra.cann.libdevice.min\": {\n+        \"replace_docstring\": [\n+            \"Computes the minimum of x and y.\",\n+        ],\n+        \"constraints\": [\n+            \"- x: ``int32`` or ``float32``\",\n+            \"- y: ``int32`` or ``float32``; must have the same type as x\",\n+            \"Return value: ``tl.tensor``, returns the minimum of x and y.\",\n+            \"Return type: same as the input type\",\n+            \"Compilation modes: SIMT\",\n+        ],\n+        \"example\":\n+        \"triton.language.extra.cann.libdevice.min\",\n+    },\n     \"triton.language.extra.cann.libdevice.mul24\": {\n         \"replace_docstring\": [\n             \"Computes the low 24-bit multiplication result of x and y.\",\n@@ -3864,6 +3931,19 @@ CONSTRAINTS = {\n         \"example\":\n         \"triton.language.extra.cann.libdevice.mulhi\",\n     },\n+    \"triton.language.extra.cann.libdevice.nan\": {\n+        \"replace_docstring\": [\n+            \"Returns a NaN value.\",\n+        ],\n+        \"constraints\": [\n+            \"- x: ``float32``\",\n+            \"Return value: ``tl.tensor``, returns NaN for every input element.\",\n+            \"Return type: ``float32``\",\n+            \"Compilation modes: SIMT\",\n+        ],\n+        \"example\":\n+        \"triton.language.extra.cann.libdevice.nan\",\n+    },\n     \"triton.language.extra.cann.libdevice.nearbyint\": {\n         \"constraints\": [\n             \"- x: ``float32``\","
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.abs.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_abs_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+\n+    return torch.abs(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_abs():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.abs(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_abs_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.acos.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_acos():\n \n     torch_res = torch.acos(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.acosh.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_acosh():\n \n     torch_res = torch.acosh(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.add_rd.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_add_rd_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.where(torch.isnan(x0) | torch.isnan(x1), float(\"nan\"), x0 + x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_add_rd():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 + x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_add_rd_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.add_rn.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_add_rn_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.where(torch.isnan(x0) | torch.isnan(x1), float(\"nan\"), x0 + x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_add_rn():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 + x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_add_rn_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.add_ru.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_add_ru_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.where(torch.isnan(x0) | torch.isnan(x1), float(\"nan\"), x0 + x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_add_ru():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 + x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_add_ru_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.add_rz.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_add_rz_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.where(torch.isnan(x0) | torch.isnan(x1), float(\"nan\"), x0 + x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_add_rz():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 + x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_add_rz_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.asin.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_asin():\n \n     torch_res = torch.asin(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.asinh.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_asinh():\n \n     torch_res = torch.asinh(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.atan.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_atan():\n \n     torch_res = torch.atan(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.atan2.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_atan2():\n \n     torch_res = torch.atan2(x0, x1)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.atanh.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_atanh():\n \n     torch_res = torch.atanh(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.brev.py",
          "status": "modified",
          "additions": 17,
          "deletions": 13,
          "patch": "@@ -16,6 +16,17 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_brev_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    result = torch.zeros_like(x0)\n+    for i in range(32):\n+        bit = (x0 >> i) & 1\n+        result = result | (bit << (31 - i))\n+    return result\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -29,22 +40,15 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n         tl.store(output + (x0), tmp1, mask=mask)\n \n \n-def _brev(x):\n-    \"\"\"Reverse the 32-bit representation of x.\"\"\"\n-    v = int(x) & 0xFFFFFFFF\n-    r = 0\n-    for _ in range(32):\n-        r = (r << 1) | (v & 1)\n-        v >>= 1\n-    return r\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_brev():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_brev(*v) for v in zip(x0.tolist())], dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_brev_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.byte_perm.py",
          "status": "modified",
          "additions": 28,
          "deletions": 16,
          "patch": "@@ -16,6 +16,24 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_byte_perm_reference(x0, x1, s):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert s.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+    assert x1.dtype == torch.int32\n+    assert s.dtype == torch.int32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == s.shape\n+\n+    result = torch.zeros_like(x0, dtype=torch.int32)\n+    for i in range(4):\n+        sel = (s >> (i * 4 + 0)) & 0x7\n+        byte = ((sel < 4) * ((x0 >> (sel * 8)) & 0xFF) + (sel >= 4) * ((x1 >> ((sel - 4) * 8)) & 0xFF))\n+        result = result | (byte << (i * 8))\n+    return result\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,25 +49,19 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n         tl.store(output + (x0), tmp3, mask=mask)\n \n \n-def _byte_perm(x, y, s):\n-    \"\"\"Select bytes from the 64-bit value (y << 32 | x) per selector s.\"\"\"\n-    combined = (int(y) << 32) | (int(x) & 0xFFFFFFFF)\n-    result = 0\n-    for i in range(4):\n-        idx = (int(s) >> (4 * i)) & 0x7\n-        result |= ((combined >> (8 * idx)) & 0xFF) << (8 * i)\n-    return result\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_byte_perm():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x2 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_byte_perm(*v) for v in zip(x0.tolist(), x1.tolist(), x2.tolist())],\n-                            dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x2 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_byte_perm_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.cbrt.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -16,6 +16,14 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_cbrt_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    sign = torch.sign(x0)\n+    abs_x = torch.abs(x0)\n+    return sign * torch.pow(abs_x, 1.0 / 3.0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_cbrt():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.cbrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_cbrt_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ceil.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ceil_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.ceil(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ceil():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.ceil(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_ceil_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.clz.py",
          "status": "modified",
          "additions": 18,
          "deletions": 9,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_clz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    vals = x0.view(-1).tolist()\n+    out = []\n+    for v in vals:\n+        u = v & 0xFFFFFFFF\n+        out.append(32 if u == 0 else 32 - u.bit_length())\n+    return torch.tensor(out, dtype=torch.int32).view(x0.shape)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -29,18 +41,15 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n         tl.store(output + (x0), tmp1, mask=mask)\n \n \n-def _clz(x):\n-    \"\"\"Count leading zero bits of the 32-bit representation of x.\"\"\"\n-    v = int(x) & 0xFFFFFFFF\n-    return 32 - v.bit_length() if v != 0 else 32\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_clz():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_clz(*v) for v in zip(x0.tolist())], dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_clz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.copysign.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_copysign():\n \n     torch_res = torch.copysign(x0, x1)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.cos.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_cos_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.cos(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_cos():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.cos(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_cos_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.cosh.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_cosh():\n \n     torch_res = torch.cosh(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.cospi.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -6,6 +6,7 @@ import os\n os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n \n import pytest\n+from scipy.special._ufuncs import _cospi as scipy_cospi\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n@@ -16,6 +17,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_cospi_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.tensor(scipy_cospi(x0.cpu().numpy())).to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_cospi():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.cos(x0 * 3.141592653589793)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_cospi_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.cyl_bessel_i0.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_cyl_bessel_i0():\n \n     torch_res = torch.i0(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.cyl_bessel_i1.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_cyl_bessel_i1_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.special.modified_bessel_i1(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_cyl_bessel_i1():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.special.i1(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_cyl_bessel_i1_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.div_rd.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_div_rd_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.div(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_div_rd():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (x0 / x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_div_rd_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.div_rn.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_div_rn_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.div(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_div_rn():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (x0 / x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_div_rn_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.div_ru.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_div_ru_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.div(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_div_ru():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (x0 / x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_div_ru_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.div_rz.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -25,9 +25,11 @@ def test_div_rz():\n     n = x0.numel()\n     out = torch.empty(n, dtype=torch.float32, device='npu')\n \n-    triton_kernel[(1, )](x0, x1, out, n, XBLOCK=n, XBLOCK_SUB=n)\n+    triton_kernel[(1, )](x0, x1, out, n, XBLOCK=n, XBLOCK_SUB=n, compile_mode='simt_only')\n \n     expected = x0 / x1\n+    out = out.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(out, expected, rtol=1e-03, atol=1e-03)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.erf.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_erf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.erf(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_erf():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.erf(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_erf_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.erfc.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_erfc_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.special.erfc(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_erfc():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.erfc(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_erfc_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.erfcinv.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_erfcinv_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.erfinv((1.0 - x0.double())).to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_erfcinv():\n-    x0 = (torch.rand((8, )) * 0.9 + 0.05).to(torch.float32).npu()\n-    expected = (torch.erfc(torch.erfcinv(x0))).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 0.9 + 0.05).to(torch.float32)\n+    expected = (torch_erfcinv_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.erfcx.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_erfcx_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.special.erfcx(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_erfcx():\n-    x0 = (torch.rand((8, )) * 1.6 - 0.8).to(torch.float32).npu()\n-    expected = (torch.exp(x0**2) * torch.erfc(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 1.6 - 0.8).to(torch.float32)\n+    expected = (torch_erfcx_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.erfinv.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_erfinv():\n \n     torch_res = torch.erfinv(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.exp.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_exp_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.exp(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_exp():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.exp(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_exp_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.exp10.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_exp10_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.pow(10, x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_exp10():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.pow(10.0, x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_exp10_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.exp2.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_exp2_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.exp2(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_exp2():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.exp2(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_exp2_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.expm1.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_expm1():\n \n     torch_res = torch.expm1(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_cosf.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_cosf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.cos(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_cosf():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.cos(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fast_cosf_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_dividef.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_fast_dividef():\n \n     torch_res = x0 / 3.0\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_exp10f.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_exp10f_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.pow(torch.tensor(10.0, dtype=torch.float32), x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_exp10f():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.pow(10.0, x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fast_exp10f_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_expf.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_fast_expf():\n \n     torch_res = torch.exp(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_log10f.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_log10f_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.log10(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_log10f():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.log10(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_fast_log10f_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_log2f.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_log2f_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.log2(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_log2f():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.log2(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_fast_log2f_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_logf.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_logf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.log(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_logf():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.log(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_fast_logf_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_powf.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_powf_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.pow(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_powf():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.pow(x0, x1)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_fast_powf_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_sinf.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_sinf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sin(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_sinf():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.sin(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fast_sinf_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_tanf.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fast_tanf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.tan(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fast_tanf():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.tan(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fast_tanf_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fast_tanhf.py",
          "status": "added",
          "additions": 55,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,55 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n+import triton\n+import triton.language as tl\n+import triton.language.extra.cann.libdevice as libdevice\n+import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_fast_tanhf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+\n+    return torch.tanh(x0)\n+\n+\n+@triton.jit\n+def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n+    offset = tl.program_id(0) * XBLOCK\n+    base = tl.arange(0, XBLOCK_SUB)\n+    loops: tl.constexpr = XBLOCK // XBLOCK_SUB\n+    for loop in range(loops):\n+        x0 = offset + (loop * XBLOCK_SUB) + base\n+        mask = x0 < n_elements\n+        tmp0 = tl.load(input0 + x0, mask=mask)\n+        tmp1 = libdevice.fast_tanhf(tmp0)\n+        tl.store(output + x0, tmp1, mask=mask)\n+\n+\n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_fast_tanhf():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = torch_fast_tanhf_reference(x0).npu()\n+    x0 = x0.npu()\n+    output = torch.empty(8, dtype=torch.float32, device='npu')\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n+\n+\n+if __name__ == \"__main__\":\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_fast_tanhf()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fdim.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fdim_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.fmax(x0 - x1, torch.tensor(0.0, dtype=x0.dtype))\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fdim():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.maximum(x0 - x1, torch.zeros_like(x0))).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fdim_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ffs.py",
          "status": "modified",
          "additions": 23,
          "deletions": 11,
          "patch": "@@ -16,6 +16,23 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ffs_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    x_vals = x0.view(-1).tolist()\n+    results = []\n+    for xv in x_vals:\n+        if xv == 0:\n+            results.append(0)\n+        else:\n+            for i in range(32):\n+                if (xv >> i) & 1:\n+                    results.append(i + 1)\n+                    break\n+    return torch.tensor(results, dtype=torch.int32).view(x0.shape)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -29,20 +46,15 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n         tl.store(output + (x0), tmp1, mask=mask)\n \n \n-def _ffs(x):\n-    \"\"\"Index of the least significant set bit (0-based), 32 if none.\"\"\"\n-    v = int(x) & 0xFFFFFFFF\n-    if v == 0:\n-        return 32\n-    return (v & -v).bit_length() - 1\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ffs():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_ffs(*v) for v in zip(x0.tolist())], dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_ffs_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.finitef.py",
          "status": "modified",
          "additions": 13,
          "deletions": 2,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_finitef_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.isfinite(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,6 +39,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+        expected = (torch_finitef_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.bool, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2half_rn.py",
          "status": "added",
          "additions": 55,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,55 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n+import triton\n+import triton.language as tl\n+import triton.language.extra.cann.libdevice as libdevice\n+import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_float2half_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+\n+    return x0.to(torch.float16)\n+\n+\n+@triton.jit\n+def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n+    offset = tl.program_id(0) * XBLOCK\n+    base = tl.arange(0, XBLOCK_SUB)\n+    loops: tl.constexpr = XBLOCK // XBLOCK_SUB\n+    for loop in range(loops):\n+        x0 = offset + (loop * XBLOCK_SUB) + base\n+        mask = x0 < n_elements\n+        tmp0 = tl.load(input0 + x0, mask=mask)\n+        tmp1 = libdevice.float2half_rn(tmp0)\n+        tl.store(output + x0, tmp1, mask=mask)\n+\n+\n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_float2half_rn():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = torch_float2half_rn_reference(x0).npu()\n+    x0 = x0.npu()\n+    output = torch.empty(8, dtype=torch.float16, device='npu')\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=0, atol=0, equal_nan=True)\n+\n+\n+if __name__ == \"__main__\":\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_float2half_rn()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2int_rd.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int32\n+\n+\n+def torch_float2int_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.floor(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2int_rd():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.floor(x0).to(torch.int32)).to(torch.int32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2int_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2int_rn.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int32\n+\n+\n+def torch_float2int_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.round(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2int_rn():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.round(x0).to(torch.int32)).to(torch.int32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2int_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2int_ru.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int32\n+\n+\n+def torch_float2int_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.ceil(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2int_ru():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.ceil(x0).to(torch.int32)).to(torch.int32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2int_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2int_rz.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int32\n+\n+\n+def torch_float2int_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2int_rz():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.trunc(x0).to(torch.int32)).to(torch.int32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2int_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ll_rd.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int64\n+\n+\n+def torch_float2ll_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.floor(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2ll_rd():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.floor(x0).to(torch.int64)).to(torch.int64).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2ll_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int64, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ll_rn.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int64\n+\n+\n+def torch_float2ll_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.round(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2ll_rn():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.round(x0).to(torch.int64)).to(torch.int64).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2ll_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int64, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ll_ru.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int64\n+\n+\n+def torch_float2ll_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.ceil(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2ll_ru():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.ceil(x0).to(torch.int64)).to(torch.int64).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2ll_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int64, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ll_rz.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.int64\n+\n+\n+def torch_float2ll_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_float2ll_rz():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.trunc(x0).to(torch.int64)).to(torch.int64).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_float2ll_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int64, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2uint_rd.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint32\n+\n+\n+def torch_float2uint_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.floor(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2uint_rd_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2uint_rn.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint32\n+\n+\n+def torch_float2uint_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.round(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2uint_rn_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2uint_ru.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint32\n+\n+\n+def torch_float2uint_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.ceil(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2uint_ru_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2uint_rz.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint32\n+\n+\n+def torch_float2uint_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2uint_rz_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ull_rd.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint64\n+\n+\n+def torch_float2ull_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2ull_rd_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint64, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ull_rn.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint64\n+\n+\n+def torch_float2ull_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.round(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2ull_rn_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint64, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ull_ru.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint64\n+\n+\n+def torch_float2ull_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.ceil(x0).to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2ull_ru_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint64, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float2ull_rz.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -15,6 +15,14 @@ from triton.backends.ascend.utils import triton_enable_libdevice_simt\n _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n+_OUT_DTYPE = torch.uint64\n+\n+\n+def torch_float2ull_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.to(_OUT_DTYPE)\n+\n \n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n@@ -33,6 +41,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = torch.abs((torch.rand((8, )) * 3.0 - 1.5).to(torch.float32))\n+        expected = (torch_float2ull_rz_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint64, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float_as_int.py",
          "status": "modified",
          "additions": 34,
          "deletions": 4,
          "patch": "@@ -1,7 +1,25 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_float_as_int_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.view(torch.int32)\n \n \n @triton.jit\n@@ -17,8 +35,20 @@ def triton_kernel(input, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: t\n         tl.store(output + (x0), tmp1, mask=mask)\n \n \n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_float_as_int():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = torch_float_as_int_reference(x0).npu()\n+    x0 = x0.npu()\n+    output = torch.empty(8, dtype=torch.int32, device='npu')\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=0, atol=0)\n+\n+\n if __name__ == \"__main__\":\n-    dtype, shape, ncore, xblock, xblock_sub = ['float32', (128, 4096), 512, 1024, 1024]\n-    input = torch.randn(shape, dtype=eval('torch.' + dtype)).npu()\n-    output = torch.zeros_like(input)\n-    triton_kernel[ncore, 1, 1](input, output, xblock, xblock_sub)\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_float_as_int()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.float_as_uint.py",
          "status": "modified",
          "additions": 13,
          "deletions": 2,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_float_as_uint_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return x0.view(torch.uint32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,6 +39,11 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n+        x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+        expected = (torch_float_as_uint_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.uint32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=0, atol=0)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.floor.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_floor_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.floor(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_floor():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.floor(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_floor_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fma.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fma_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return x0 * x1 + x2\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fma():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1 + x2).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fma_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fma_rd.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fma_rd_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return x0 * x1 + x2\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fma_rd():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1 + x2).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fma_rd_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fma_rn.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fma_rn_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return x0 * x1 + x2\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fma_rn():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1 + x2).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fma_rn_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fma_ru.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fma_ru_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return x0 * x1 + x2\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fma_ru():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1 + x2).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fma_ru_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fma_rz.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_fma_rz_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return x0 * x1 + x2\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_fma_rz():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1 + x2).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_fma_rz_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.fmod.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_fmod():\n \n     torch_res = torch.fmod(x0, x1)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.gamma.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_gamma():\n \n     torch_res = torch.exp(torch.lgamma(x0))\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.hadd.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_hadd_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == x1.dtype\n+    assert x0.dtype in (torch.int32, torch.uint32)\n+    assert x0.shape == x1.shape\n+\n+    return ((x0.to(torch.int64) + x1.to(torch.int64)) >> 1).to(x0.dtype)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_hadd():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = ((x0 + x1) // 2).to(torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_hadd_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.half2float.py",
          "status": "added",
          "additions": 55,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,55 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n+import triton\n+import triton.language as tl\n+import triton.language.extra.cann.libdevice as libdevice\n+import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_half2float_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float16\n+\n+    return x0.to(torch.float32)\n+\n+\n+@triton.jit\n+def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n+    offset = tl.program_id(0) * XBLOCK\n+    base = tl.arange(0, XBLOCK_SUB)\n+    loops: tl.constexpr = XBLOCK // XBLOCK_SUB\n+    for loop in range(loops):\n+        x0 = offset + (loop * XBLOCK_SUB) + base\n+        mask = x0 < n_elements\n+        tmp0 = tl.load(input0 + x0, mask=mask)\n+        tmp1 = libdevice.half2float(tmp0)\n+        tl.store(output + x0, tmp1, mask=mask)\n+\n+\n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_half2float():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float16)\n+    expected = torch_half2float_reference(x0).npu()\n+    x0 = x0.npu()\n+    output = torch.empty(8, dtype=torch.float32, device='npu')\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=0, atol=0, equal_nan=True)\n+\n+\n+if __name__ == \"__main__\":\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_half2float()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.hypot.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_hypot():\n \n     torch_res = torch.hypot(x0, x1)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ilogb.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_ilogb():\n \n     torch_res = torch.floor(torch.log2(torch.abs(x0))).to(torch.int32)\n     triton_res = torch.empty_like(torch_res)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=0, atol=0, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.int2float_rd.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_int2float_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_int2float_rd():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_int2float_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.int2float_rn.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_int2float_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_int2float_rn():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_int2float_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.int2float_ru.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_int2float_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_int2float_ru():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_int2float_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.int2float_rz.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_int2float_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_int2float_rz():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_int2float_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.int_as_float.py",
          "status": "modified",
          "additions": 15,
          "deletions": 2,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_int_as_float_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    return x0.view(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,6 +40,12 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n+        x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+        expected = (torch_int_as_float_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.float32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n+        assert torch.equal(output.cpu().view(torch.int32), expected.cpu().view(torch.int32))"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.isinf.py",
          "status": "modified",
          "additions": 4,
          "deletions": 2,
          "patch": "@@ -27,9 +27,11 @@ def test_isinf():\n \n     torch_res = torch.isinf(x0).to(torch.int32)\n     triton_res = torch.empty(shape, dtype=torch.int32, device='npu')\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n-    torch.testing.assert_close(torch_res.cpu(), triton_res.cpu(), rtol=0, atol=0)\n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n+    torch.testing.assert_close(torch_res, triton_res, rtol=0, atol=0)\n \n \n if __name__ == \"__main__\":"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.isnan.py",
          "status": "modified",
          "additions": 4,
          "deletions": 2,
          "patch": "@@ -27,9 +27,11 @@ def test_isnan():\n \n     torch_res = torch.isnan(x0).to(torch.int32)\n     triton_res = torch.empty(shape, dtype=torch.int32, device='npu')\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n-    torch.testing.assert_close(torch_res.cpu(), triton_res.cpu(), rtol=0, atol=0)\n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n+    torch.testing.assert_close(torch_res, triton_res, rtol=0, atol=0)\n \n \n if __name__ == \"__main__\":"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.j0.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -16,6 +16,14 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_j0_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    from scipy import special\n+\n+    return torch.tensor(special.j0(x0.numpy()))\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_j0():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.special.bessel_j0(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_j0_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.j1.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -16,6 +16,14 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_j1_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    from scipy import special\n+\n+    return torch.tensor(special.j1(x0.numpy()))\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_j1():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.special.bessel_j1(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_j1_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.jn.py",
          "status": "modified",
          "additions": 60,
          "deletions": 3,
          "patch": "@@ -6,6 +6,7 @@ import os\n os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n \n import pytest\n+import numpy as np\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n@@ -16,6 +17,56 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_jn_reference(x0, x1):\n+    from scipy import special\n+\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    n_np = x0.numpy()\n+    x_np = x1.numpy()\n+\n+    # SciPy implements the mathematical jn, but libdevice returns NaN for\n+    # n < 0; substitute 0 and mask to NaN afterwards.\n+    n_for_scipy = np.where(n_np < 0, 0, n_np)\n+\n+    with np.errstate(all=\"ignore\"):\n+        y_np = special.jn(n_for_scipy, x_np)\n+\n+    y_np = np.asarray(y_np)\n+\n+    mask_n_neg = n_np < 0\n+    mask_x_nan = np.isnan(x_np)\n+    mask_x_posinf = np.isposinf(x_np)\n+    mask_x_neginf = np.isneginf(x_np)\n+\n+    # Aligned with measured device behavior: jn(n, 0), n >= 2 -> NaN.\n+    mask_x_zero_n_ge_2 = (x_np == 0.0) & (n_np >= 2)\n+\n+    # +inf -> +0\n+    y_np = np.where(mask_x_posinf & ~mask_n_neg, 0.0, y_np)\n+\n+    # -inf: even n -> +0, odd n -> -0\n+    mask_neginf_even = mask_x_neginf & ~mask_n_neg & ((n_np % 2) == 0)\n+    mask_neginf_odd = mask_x_neginf & ~mask_n_neg & ((n_np % 2) == 1)\n+\n+    y_np = np.where(mask_neginf_even, 0.0, y_np)\n+    y_np = np.where(mask_neginf_odd, -0.0, y_np)\n+\n+    # Keep the signed zero of J_1(+-0.0).\n+    mask_j1_zero = (n_np == 1) & (x_np == 0.0)\n+    y_np = np.where(mask_j1_zero, x_np, y_np)\n+\n+    # NaN last.\n+    mask_nan = mask_n_neg | mask_x_nan | mask_x_zero_n_ge_2\n+    y_np = np.where(mask_nan, np.nan, y_np)\n+\n+    return torch.as_tensor(y_np, dtype=torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -34,7 +85,13 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-        x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n+        x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+        x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+        expected = (torch_jn_reference(x0, x1)).npu()\n+        x0 = x0.npu()\n+        x1 = x1.npu()\n         output = torch.empty(8, dtype=torch.float32, device='npu')\n-        triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ldexp.py",
          "status": "modified",
          "additions": 18,
          "deletions": 10,
          "patch": "@@ -1,9 +1,17 @@\n-import pytest\n import torch\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n-from triton.backends.ascend.utils import is_compile_on_910_95\n+\n+\n+def torch_ldexp_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.int32\n+    assert x0.shape == x1.shape\n+\n+    return torch.ldexp(x0, x1)\n \n \n @triton.jit\n@@ -19,19 +27,19 @@ def triton_ldexp(in_ptr0, in_ptr1, out_ptr0, xnumel, XBLOCK: tl.constexpr, XBLOC\n \n \n def test_ldexp():\n-    if is_compile_on_910_95():\n-        # TODO: re-enable once bisheng fixes __hmf_ldexpf on Ascend 950 — the\n-        # intrinsic currently returns wrong values (results overflow to inf).\n-        pytest.skip(\"ldexp is currently broken on Ascend 950\")\n     shape = (2, 256)\n     ncore, xblock, xblock_sub = 2, 1024, 512\n-    x0 = torch.randn(size=shape, dtype=torch.float32).npu()\n-    x1 = torch.randint(-126, 128, size=shape, dtype=torch.int32, device='npu')\n+    x0 = torch.randn(size=shape, dtype=torch.float32)\n+    x1 = torch.randint(-126, 128, size=shape, dtype=torch.int32)\n+    torch_res = torch_ldexp_reference(x0, x1)\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n \n-    torch_res = x0 * (2.0**x1.float())\n     triton_res = torch.empty_like(x0)\n-    triton_ldexp[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_ldexp[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.lgamma.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_lgamma():\n \n     torch_res = torch.lgamma(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ll2float_rd.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ll2float_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ll2float_rd():\n-    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64)\n+    expected = (torch_ll2float_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ll2float_rn.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ll2float_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ll2float_rn():\n-    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64)\n+    expected = (torch_ll2float_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ll2float_ru.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ll2float_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ll2float_ru():\n-    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64)\n+    expected = (torch_ll2float_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ll2float_rz.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ll2float_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ll2float_rz():\n-    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(1, 16, (8, ), dtype=torch.int64)).to(torch.int64)\n+    expected = (torch_ll2float_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.llrint.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_llrint_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.round(x0).to(torch.int64)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_llrint():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.round(x0).to(torch.int64)).to(torch.int64).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_llrint_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int64, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.llround.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_llround_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.round(x0).to(torch.int64)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_llround():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.round(x0).to(torch.int64)).to(torch.int64).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_llround_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int64, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.log.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_log_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.log(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_log():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.log(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_log_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.log10.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_log10():\n \n     torch_res = torch.log10(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.log1p.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_log1p():\n \n     torch_res = torch.log1p(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.log2.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_log2_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.log2(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_log2():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.log2(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_log2_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.logb.py",
          "status": "modified",
          "additions": 18,
          "deletions": 5,
          "patch": "@@ -6,6 +6,7 @@ import os\n os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n \n import pytest\n+import math\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n@@ -16,6 +17,15 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_logb_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    original_shape = x0.shape\n+    exponents = [math.frexp(value.item())[1] - 1 if value.item() != 0 else 0 for value in x0.flatten()]\n+    result = torch.tensor(exponents, dtype=torch.int32).reshape(original_shape)\n+    return result\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,11 +41,14 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_logb():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.floor(torch.log2(torch.abs(x0))).to(torch.int32)).to(torch.int32).npu()\n-    output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n-    torch.testing.assert_close(output, expected, rtol=0, atol=0)\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_logb_reference(x0).to(torch.float32)).npu()\n+    x0 = x0.npu()\n+    output = torch.empty(8, dtype=torch.float32, device='npu')\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n \n if __name__ == \"__main__\":"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.max.py",
          "status": "added",
          "additions": 61,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,61 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n+import triton\n+import triton.language as tl\n+import triton.language.extra.cann.libdevice as libdevice\n+import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_max_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.fmax(x0, x1)\n+\n+\n+@triton.jit\n+def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n+    offset = tl.program_id(0) * XBLOCK\n+    base = tl.arange(0, XBLOCK_SUB)\n+    loops: tl.constexpr = XBLOCK // XBLOCK_SUB\n+    for loop in range(loops):\n+        x0 = offset + (loop * XBLOCK_SUB) + base\n+        mask = x0 < n_elements\n+        tmp0 = tl.load(input0 + x0, mask=mask)\n+        tmp1 = tl.load(input1 + x0, mask=mask)\n+        tmp2 = libdevice.max(tmp0, tmp1)\n+        tl.store(output + x0, tmp2, mask=mask)\n+\n+\n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_max():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = torch_max_reference(x0, x1).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    output = torch.empty(8, dtype=torch.float32, device='npu')\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=1e-04, atol=1e-04, equal_nan=True)\n+\n+\n+if __name__ == \"__main__\":\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_max()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.min.py",
          "status": "added",
          "additions": 61,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,61 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n+import triton\n+import triton.language as tl\n+import triton.language.extra.cann.libdevice as libdevice\n+import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_min_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.fmin(x0, x1)\n+\n+\n+@triton.jit\n+def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n+    offset = tl.program_id(0) * XBLOCK\n+    base = tl.arange(0, XBLOCK_SUB)\n+    loops: tl.constexpr = XBLOCK // XBLOCK_SUB\n+    for loop in range(loops):\n+        x0 = offset + (loop * XBLOCK_SUB) + base\n+        mask = x0 < n_elements\n+        tmp0 = tl.load(input0 + x0, mask=mask)\n+        tmp1 = tl.load(input1 + x0, mask=mask)\n+        tmp2 = libdevice.min(tmp0, tmp1)\n+        tl.store(output + x0, tmp2, mask=mask)\n+\n+\n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_min():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = torch_min_reference(x0, x1).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    output = torch.empty(8, dtype=torch.float32, device='npu')\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=1e-04, atol=1e-04, equal_nan=True)\n+\n+\n+if __name__ == \"__main__\":\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_min()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.mul24.py",
          "status": "modified",
          "additions": 20,
          "deletions": 13,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_mul24_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == x1.dtype\n+    assert x0.dtype in (torch.int32, torch.uint32)\n+    assert x0.shape == x1.shape\n+\n+    if x0.dtype == torch.int32:\n+        return ((x0 << 8) >> 8) * ((x1 << 8) >> 8)\n+    return (((x0.to(torch.int64) & 0xFFFFFF) * (x1.to(torch.int64) & 0xFFFFFF)) & 0xFFFFFFFF).to(torch.uint32)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -30,22 +42,17 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n         tl.store(output + (x0), tmp2, mask=mask)\n \n \n-def _mul24(x, y):\n-    \"\"\"Low 24-bit multiplication result.\"\"\"\n-    v = (int(x) & 0xFFFFFF) * (int(y) & 0xFFFFFF)\n-    v &= 0xFFFFFF\n-    if v & 0x800000:\n-        v -= 0x1000000\n-    return v\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_mul24():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_mul24(*v) for v in zip(x0.tolist(), x1.tolist())], dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_mul24_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.mul_rd.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_mul_rd_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return x0 * x1\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_mul_rd():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_mul_rd_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.mul_rn.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_mul_rn_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return x0 * x1\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_mul_rn():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_mul_rn_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.mul_ru.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_mul_ru_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return x0 * x1\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_mul_ru():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_mul_ru_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.mul_rz.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_mul_rz_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return x0 * x1\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_mul_rz():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 * x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_mul_rz_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.mulhi.py",
          "status": "modified",
          "additions": 19,
          "deletions": 11,
          "patch": "@@ -16,6 +16,17 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_mulhi_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+    assert x1.dtype == torch.int32\n+    assert x0.shape == x1.shape\n+\n+    product = x0.to(torch.int64) * x1.to(torch.int64)\n+    return (product >> 32).to(torch.int32)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -30,20 +41,17 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n         tl.store(output + (x0), tmp2, mask=mask)\n \n \n-def _mulhi(x, y):\n-    \"\"\"High 32 bits of the 64-bit product of two 32-bit integers.\"\"\"\n-    p = (int(x) * int(y)) & 0xFFFFFFFFFFFFFFFF\n-    v = p >> 32\n-    return v - 0x100000000 if v & 0x80000000 else v\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_mulhi():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_mulhi(*v) for v in zip(x0.tolist(), x1.tolist())], dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_mulhi_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.nan.py",
          "status": "added",
          "additions": 56,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,56 @@\n+import os\n+\n+# The libdevice SIMT ops below are A5-only (Ascend 910_95 / 950) and are\n+# additionally gated by this env switch; set it so the examples run on A5\n+# hardware without extra configuration.\n+os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n+\n+import pytest\n+import triton\n+import triton.language as tl\n+import triton.language.extra.cann.libdevice as libdevice\n+import torch\n+from triton.backends.ascend.utils import triton_enable_libdevice_simt\n+\n+_SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n+                  \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n+\n+\n+def torch_nan_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+\n+    return torch.full(x0.shape, float(\"nan\"), dtype=torch.float32)\n+\n+\n+@triton.jit\n+def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n+    offset = tl.program_id(0) * XBLOCK\n+    base = tl.arange(0, XBLOCK_SUB)\n+    loops: tl.constexpr = XBLOCK // XBLOCK_SUB\n+    for loop in range(loops):\n+        x0 = offset + (loop * XBLOCK_SUB) + base\n+        mask = x0 < n_elements\n+        tmp0 = tl.load(input0 + x0, mask=mask)\n+        tmp1 = libdevice.nan(tmp0)\n+        tl.store(output + x0, tmp1, mask=mask)\n+\n+\n+@pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n+def test_nan():\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = torch_nan_reference(x0).npu()\n+    x0 = x0.npu()\n+    output = torch.empty(8, dtype=torch.float32, device='npu')\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n+    torch.testing.assert_close(output, expected, rtol=0, atol=0, equal_nan=True)\n+    assert torch.isnan(output.cpu()).all()\n+\n+\n+if __name__ == \"__main__\":\n+    if not triton_enable_libdevice_simt():\n+        print(_SIMT_SKIP_MSG)\n+    else:\n+        test_nan()"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.nearbyint.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_nearbyint():\n \n     torch_res = torch.round(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.nextafter.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_nextafter():\n \n     torch_res = torch.nextafter(x0, x1)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.norm3d.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_norm3d_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return torch.sqrt(x0 * x0 + x1 * x1 + x2 * x2)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_norm3d():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0 * x0 + x1 * x1 + x2 * x2)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_norm3d_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.norm4d.py",
          "status": "modified",
          "additions": 27,
          "deletions": 6,
          "patch": "@@ -16,6 +16,21 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_norm4d_reference(x0, x1, x2, x3):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x3.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x3.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    assert x2.shape == x3.shape\n+    return torch.sqrt(x0 * x0 + x1 * x1 + x2 * x2 + x3 * x3)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, input3, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -34,13 +49,19 @@ def triton_kernel(input0, input1, input2, input3, output, n_elements, XBLOCK: tl\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_norm4d():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x3 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0 * x0 + x1 * x1 + x2 * x2 + x3 * x3)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x3 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_norm4d_reference(x0, x1, x2, x3)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n+    x3 = x3.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, x3, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, x3, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.normcdf.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_normcdf_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.special.ndtr(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_normcdf():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (0.5 * (1 + torch.erf(x0 / 1.4142135623730951))).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_normcdf_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.normcdfinv.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_normcdfinv_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    x0_float32 = x0.to(torch.float32)\n+    return torch.special.ndtri(x0_float32).to(x0.dtype)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_normcdfinv():\n-    x0 = (torch.rand((8, )) * 0.9 + 0.05).to(torch.float32).npu()\n-    expected = (1.4142135623730951 * torch.erfinv(2 * x0 - 1)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 0.9 + 0.05).to(torch.float32)\n+    expected = (torch_normcdfinv_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.popc.py",
          "status": "modified",
          "additions": 17,
          "deletions": 8,
          "patch": "@@ -16,6 +16,17 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_popc_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+\n+    result = torch.zeros(x0.shape, dtype=torch.int32)\n+    for i in range(32):\n+        bit = ((x0 >> i) & 1).to(torch.int32)\n+        result = result + bit\n+    return result\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -29,17 +40,15 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n         tl.store(output + (x0), tmp1, mask=mask)\n \n \n-def _popc(x):\n-    \"\"\"Number of bits set to 1 in the 32-bit representation of x.\"\"\"\n-    return bin(int(x) & 0xFFFFFFFF).count(\"1\")\n-\n-\n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_popc():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = torch.tensor([_popc(*v) for v in zip(x0.tolist())], dtype=torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_popc_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.pow.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -26,8 +26,10 @@ def test_pow():\n \n     torch_res = torch.pow(x0, x1)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, x1, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rcbrt.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -6,6 +6,7 @@ import os\n os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n \n import pytest\n+from scipy.special import cbrt\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n@@ -16,6 +17,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rcbrt_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.tensor(1.0 / cbrt(x0.numpy()), dtype=x0.dtype, device=x0.device)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rcbrt():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (1.0 / torch.cbrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rcbrt_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rcp_rd.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rcp_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return 1.0 / x0\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rcp_rd():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (1.0 / x0).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rcp_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rcp_rn.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rcp_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return 1.0 / x0\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rcp_rn():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (1.0 / x0).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rcp_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rcp_ru.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rcp_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return 1.0 / x0\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rcp_ru():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (1.0 / x0).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rcp_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rcp_rz.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rcp_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return 1.0 / x0\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rcp_rz():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (1.0 / x0).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rcp_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.reciprocal.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_reciprocal():\n \n     torch_res = torch.reciprocal(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.relu.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_relu():\n \n     torch_res = torch.relu(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.remainder.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_remainder_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.remainder(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_remainder():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.remainder(x0, x1)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_remainder_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rhadd.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rhadd_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == x1.dtype\n+    assert x0.dtype in (torch.int32, torch.uint32)\n+    assert x0.shape == x1.shape\n+\n+    return ((x0.to(torch.int64) + x1.to(torch.int64) + 1) >> 1).to(x0.dtype)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rhadd():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = ((x0 + x1) // 2).to(torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_rhadd_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rhypot.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rhypot_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return 1.0 / torch.hypot(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rhypot():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (1.0 / torch.hypot(x0, x1)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rhypot_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rint.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_rint():\n \n     torch_res = torch.round(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rnorm3d.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rnorm3d_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    return torch.rsqrt(x0 * x0 + x1 * x1 + x2 * x2)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rnorm3d():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (1.0 / torch.sqrt(x0 * x0 + x1 * x1 + x2 * x2)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_rnorm3d_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rnorm4d.py",
          "status": "modified",
          "additions": 27,
          "deletions": 6,
          "patch": "@@ -16,6 +16,21 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rnorm4d_reference(x0, x1, x2, x3):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x3.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x2.dtype == torch.float32\n+    assert x3.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+    assert x1.shape == x2.shape\n+    assert x2.shape == x3.shape\n+    return torch.rsqrt(x0 * x0 + x1 * x1 + x2 * x2 + x3 * x3)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, input3, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -34,13 +49,19 @@ def triton_kernel(input0, input1, input2, input3, output, n_elements, XBLOCK: tl\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rnorm4d():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x3 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (1.0 / torch.sqrt(x0 * x0 + x1 * x1 + x2 * x2 + x3 * x3)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x2 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x3 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_rnorm4d_reference(x0, x1, x2, x3)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n+    x3 = x3.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, x3, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, x3, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.round.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_round():\n \n     torch_res = torch.round(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rsqrt.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rsqrt_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return 1.0 / torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rsqrt():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.rsqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rsqrt_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.rsqrt_rn.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_rsqrt_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return 1.0 / torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_rsqrt_rn():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.rsqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_rsqrt_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sad.py",
          "status": "modified",
          "additions": 22,
          "deletions": 5,
          "patch": "@@ -16,6 +16,18 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sad_reference(x0, x1, x2):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x2.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+    assert x1.dtype == torch.int32\n+    assert x2.dtype == torch.uint32\n+    assert x0.shape == x1.shape == x2.shape\n+\n+    return (torch.abs(x0 - x1).to(torch.int64) + x2.to(torch.int64)).to(torch.int32)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,12 +45,17 @@ def triton_kernel(input0, input1, input2, output, n_elements, XBLOCK: tl.constex\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sad():\n-    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    x2 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = (torch.abs(x0 - x1) + x2).to(torch.int32).npu()\n+    x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    x2 = (torch.randint(1, 16, (8, ))).to(torch.uint32)\n+    expected = (torch_sad_reference(x0, x1, x2)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n+    x2 = x2.npu()\n     output = torch.empty(8, dtype=torch.int32, device='npu')\n-    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, x2, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=0, atol=0)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.saturatef.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_saturatef_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+\n+    return torch.clamp(x0, 0.0, 1.0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_saturatef():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.clamp(x0, 0.0, 1.0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_saturatef_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.scalbn.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_scalbn_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.int32\n+    assert x0.shape == x1.shape\n+\n+    return torch.ldexp(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_scalbn():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-    expected = (torch.ldexp(x0, x1)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    x1 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+    expected = (torch_scalbn_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.signbit.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_signbit():\n \n     torch_res = torch.signbit(x0).to(torch.int32)\n     triton_res = torch.empty(shape, dtype=torch.int32, device='npu')\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=0, atol=0, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sin.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sin_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sin(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sin():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.sin(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_sin_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sinh.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_sinh():\n \n     torch_res = torch.sinh(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sinpi.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -6,6 +6,7 @@ import os\n os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n \n import pytest\n+from scipy.special._ufuncs import _sinpi as scipy_sinpi\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n@@ -16,6 +17,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sinpi_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.tensor(scipy_sinpi(x0.cpu().numpy())).to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sinpi():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (torch.sin(x0 * 3.141592653589793)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_sinpi_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sqrt.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sqrt_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sqrt():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_sqrt_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sqrt_rd.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sqrt_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sqrt_rd():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_sqrt_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sqrt_rn.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sqrt_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sqrt_rn():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_sqrt_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sqrt_ru.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sqrt_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sqrt_ru():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_sqrt_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sqrt_rz.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sqrt_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    return torch.sqrt(x0)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sqrt_rz():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.sqrt(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_sqrt_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sub_rd.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sub_rd_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.sub(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sub_rd():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 - x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_sub_rd_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sub_rn.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sub_rn_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.sub(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sub_rn():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 - x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_sub_rn_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sub_ru.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sub_ru_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.sub(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sub_ru():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 - x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_sub_ru_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.sub_rz.py",
          "status": "modified",
          "additions": 18,
          "deletions": 4,
          "patch": "@@ -16,6 +16,16 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_sub_rz_reference(x0, x1):\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    return torch.sub(x0, x1)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -32,11 +42,15 @@ def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLO\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_sub_rz():\n-    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32).npu()\n-    expected = (x0 - x1).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    x1 = (torch.rand((8, )) * 3.0 - 1.5).to(torch.float32)\n+    expected = (torch_sub_rz_reference(x0, x1)).npu()\n+    x0 = x0.npu()\n+    x1 = x1.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-02, atol=1e-02, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.tan.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_tan():\n \n     torch_res = torch.tan(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.tanh.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_tanh():\n \n     torch_res = torch.tanh(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.tgamma.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -16,6 +16,14 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_tgamma_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    from scipy.special import gamma\n+\n+    return torch.tensor(gamma(x0.cpu().numpy()), dtype=torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_tgamma():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.exp(torch.lgamma(x0))).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_tgamma_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.trunc.py",
          "status": "modified",
          "additions": 3,
          "deletions": 1,
          "patch": "@@ -24,8 +24,10 @@ def test_trunc():\n \n     torch_res = torch.trunc(x0)\n     triton_res = torch.empty_like(x0)\n-    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub)\n+    triton_kernel[ncore, 1, 1](x0, triton_res, x0.numel(), xblock, xblock_sub, compile_mode='simt_only')\n \n+    torch_res = torch_res.cpu()\n+    triton_res = triton_res.cpu()\n     torch.testing.assert_close(torch_res, triton_res, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.uint2float_rd.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_uint2float_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_uint2float_rd():\n-    x0 = (torch.randint(0, 100, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ))).to(torch.uint32)\n+    expected = (torch_uint2float_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.uint2float_rn.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_uint2float_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_uint2float_rn():\n-    x0 = (torch.randint(0, 100, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ))).to(torch.uint32)\n+    expected = (torch_uint2float_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.uint2float_ru.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_uint2float_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_uint2float_ru():\n-    x0 = (torch.randint(0, 100, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ))).to(torch.uint32)\n+    expected = (torch_uint2float_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.uint2float_rz.py",
          "status": "modified",
          "additions": 13,
          "deletions": 3,
          "patch": "@@ -16,6 +16,13 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_uint2float_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint32\n+\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +38,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_uint2float_rz():\n-    x0 = (torch.randint(0, 100, (8, ))).to(torch.int32).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ))).to(torch.uint32)\n+    expected = (torch_uint2float_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.uint_as_float.py",
          "status": "modified",
          "additions": 14,
          "deletions": 2,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_uint_as_float_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint32\n+    return x0.view(torch.int32).view(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -33,6 +39,12 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.randint(0, 100, (8, ))).to(torch.int32).npu()\n+        x0 = (torch.randint(0, 100, (8, ))).to(torch.uint32)\n+        expected = (torch_uint_as_float_reference(x0)).npu()\n+        x0 = x0.npu()\n         output = torch.empty(8, dtype=torch.float32, device='npu')\n-        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n+        assert torch.equal(output.cpu().view(torch.int32), expected.cpu().view(torch.int32))"
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ull2float_rd.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ull2float_rd_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ull2float_rd():\n-    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.uint64)\n+    expected = (torch_ull2float_rd_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ull2float_rn.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ull2float_rn_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ull2float_rn():\n-    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.uint64)\n+    expected = (torch_ull2float_rn_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ull2float_ru.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ull2float_ru_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ull2float_ru():\n-    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.uint64)\n+    expected = (torch_ull2float_ru_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.ull2float_rz.py",
          "status": "modified",
          "additions": 12,
          "deletions": 3,
          "patch": "@@ -16,6 +16,12 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_ull2float_rz_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.uint64\n+    return x0.to(torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +37,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_ull2float_rz():\n-    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.int64).npu()\n-    expected = (x0.to(torch.float32)).to(torch.float32).npu()\n+    x0 = (torch.randint(0, 100, (8, ), dtype=torch.int64)).to(torch.uint64)\n+    expected = (torch_ull2float_rz_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.y0.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -16,6 +16,14 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_y0_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    from scipy import special\n+\n+    return torch.tensor(special.y0(x0.cpu().numpy()), dtype=torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_y0():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.special.bessel_y0(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_y0_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.y1.py",
          "status": "modified",
          "additions": 14,
          "deletions": 3,
          "patch": "@@ -16,6 +16,14 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_y1_reference(x0):\n+    assert x0.device.type == \"cpu\"\n+    assert x0.dtype == torch.float32\n+    from scipy import special\n+\n+    return torch.tensor(special.y1(x0.cpu().numpy()), dtype=torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -31,10 +39,13 @@ def triton_kernel(input0, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB:\n \n @pytest.mark.skipif(not triton_enable_libdevice_simt(), reason=_SIMT_SKIP_MSG)\n def test_y1():\n-    x0 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n-    expected = (torch.special.bessel_y1(x0)).to(torch.float32).npu()\n+    x0 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+    expected = (torch_y1_reference(x0)).npu()\n+    x0 = x0.npu()\n     output = torch.empty(8, dtype=torch.float32, device='npu')\n-    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+    triton_kernel[(1, )](x0, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+    output = output.cpu()\n+    expected = expected.cpu()\n     torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)\n \n "
        },
        {
          "filename": "docs/zh/python-api/_examples/triton.language.extra.cann.libdevice.yn.py",
          "status": "modified",
          "additions": 47,
          "deletions": 3,
          "patch": "@@ -6,6 +6,7 @@ import os\n os.environ.setdefault(\"TRITON_ENABLE_LIBDEVICE_SIMT\", \"1\")\n \n import pytest\n+import numpy as np\n import triton\n import triton.language as tl\n import triton.language.extra.cann.libdevice as libdevice\n@@ -16,6 +17,43 @@ _SIMT_SKIP_MSG = (\"SIMT libdevice ops require an Ascend 950 target \"\n                   \"with TRITON_ENABLE_LIBDEVICE_SIMT=1; skipping.\")\n \n \n+def torch_yn_reference(x0, x1):\n+    from scipy import special\n+\n+    assert x0.device.type == \"cpu\"\n+    assert x1.device.type == \"cpu\"\n+    assert x0.dtype == torch.int32\n+    assert x1.dtype == torch.float32\n+    assert x0.shape == x1.shape\n+\n+    n_np = x0.numpy()\n+    x_np = x1.numpy()\n+\n+    # SciPy implements the mathematical yn, but libdevice returns NaN for\n+    # n < 0; substitute 0 and mask to NaN afterwards.\n+    n_for_scipy = np.where(n_np < 0, 0, n_np)\n+\n+    with np.errstate(all=\"ignore\"):\n+        y_np = special.yn(n_for_scipy, x_np)\n+\n+    y_np = np.asarray(y_np)\n+\n+    mask_n_neg = n_np < 0\n+    mask_x_nan = np.isnan(x_np)\n+    mask_x_neg = x_np < 0.0\n+    mask_x_zero = x_np == 0.0\n+    mask_x_posinf = np.isposinf(x_np)\n+\n+    # n >= 0, 0 < x < +inf: yn(n, x)\n+    y_np = np.where(mask_x_zero & ~mask_n_neg, -np.inf, y_np)\n+    y_np = np.where(mask_x_posinf & ~mask_n_neg, 0.0, y_np)\n+\n+    mask_nan = mask_n_neg | mask_x_neg | mask_x_nan\n+    y_np = np.where(mask_nan, np.nan, y_np)\n+\n+    return torch.as_tensor(y_np, dtype=torch.float32)\n+\n+\n @triton.jit\n def triton_kernel(input0, input1, output, n_elements, XBLOCK: tl.constexpr, XBLOCK_SUB: tl.constexpr):\n     offset = tl.program_id(0) * XBLOCK\n@@ -34,7 +72,13 @@ if __name__ == \"__main__\":\n     if not triton_enable_libdevice_simt():\n         print(_SIMT_SKIP_MSG)\n     else:\n-        x0 = (torch.randint(1, 16, (8, ))).to(torch.int32).npu()\n-        x1 = (torch.rand((8, )) + 0.1).to(torch.float32).npu()\n+        x0 = (torch.randint(1, 16, (8, ))).to(torch.int32)\n+        x1 = (torch.rand((8, )) + 0.1).to(torch.float32)\n+        expected = (torch_yn_reference(x0, x1)).npu()\n+        x0 = x0.npu()\n+        x1 = x1.npu()\n         output = torch.empty(8, dtype=torch.float32, device='npu')\n-        triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, force_simt_only=True)\n+        triton_kernel[(1, )](x0, x1, output, 8, XBLOCK=8, XBLOCK_SUB=8, compile_mode='simt_only')\n+        output = output.cpu()\n+        expected = expected.cpu()\n+        torch.testing.assert_close(output, expected, rtol=1e-03, atol=1e-03, equal_nan=True)"
        },
        {
          "filename": "docs/zh/python-api/triton.language.extra.cann.libdevice.rst",
          "status": "modified",
          "additions": 6,
          "deletions": 0,
          "patch": "@@ -58,9 +58,11 @@ Libdevice Ops\n     fast_powf\n     fast_sinf\n     fast_tanf\n+    fast_tanhf\n     fdim\n     ffs\n     finitef\n+    float2half_rn\n     float2int_rd\n     float2int_rn\n     float2int_ru\n@@ -88,6 +90,7 @@ Libdevice Ops\n     fmod\n     gamma\n     hadd\n+    half2float\n     hypot\n     ilogb\n     int2float_rd\n@@ -113,12 +116,15 @@ Libdevice Ops\n     log1p\n     log2\n     logb\n+    max\n+    min\n     mul24\n     mul_rd\n     mul_rn\n     mul_ru\n     mul_rz\n     mulhi\n+    nan\n     nearbyint\n     nextafter\n     norm3d\n"
        }
      ]
    },
    {
      "sha": "645c748bc01d2e46d4e6fb8ecc796f3a088f3fb4",
      "author": {
        "name": "jiahao su",
        "email": "damow890@gmail.com"
      },
      "date": "2026-09-02T16:12:34+08:00",
      "message": "Enable the yellow zone pipeline function for the mixed branch (#1911)",
      "parents": [
        "e9f9b169f8ffd6dda35a6853135c83d7e66df25f"
      ],
      "stats": {
        "total_additions": 382,
        "total_deletions": 0,
        "files_changed": 1
      },
      "files": [
        {
          "filename": ".github/workflows/Ascend950-ci.yml",
          "status": "added",
          "additions": 382,
          "deletions": 0,
          "patch": "new file mode 100644\n@@ -0,0 +1,382 @@\n+name: Ascend950 CI\n+\n+on:\n+  pull_request_target:\n+    branches: ['feature/simd-simt-compile-mode']\n+    paths: &paths\n+      # Source code\n+      - 'python/**'\n+      - 'lib/**'\n+      - 'include/**'\n+      - 'bin/**'\n+      - 'third_party/**'\n+      - 'test/**'\n+      - 'unittest/**'\n+      - 'utils/**'\n+      - 'scripts/**'\n+      # Build configuration\n+      - 'cmake/**'\n+      - 'CMakeLists.txt'\n+      - 'setup.py'\n+      - 'pyproject.toml'\n+      - 'Makefile'\n+      - 'safe_compile.cmake'\n+      - 'version.txt'\n+      # CI itself\n+      - '.github/workflows/Ascend950-ci.yml'\n+\n+concurrency:\n+  # PR runs key on PR number (cancel earlier pushes); branch-push runs key on\n+  # SHA so distinct merges don't cancel each other.\n+  group: ascend950-${{ github.event_name == 'pull_request_target' && github.event.pull_request.number || github.sha }}\n+  cancel-in-progress: ${{ github.event_name == 'pull_request_target' }}\n+\n+permissions: {}\n+\n+jobs:\n+  # ── Wheels Build ──────────────────────────────────────────────────────────\n+  # Security: under pull_request_target, the workflow file from the base\n+  # branch is used. This job checks out the PR head SHA but does NOT receive\n+  # any secrets in its env/steps — the only credential available is the\n+  # GITHUB_TOKEN scoped to contents:read. PR code (setup.py) cannot exfiltrate\n+  # secrets.\n+\n+  wheels-build:\n+    name: Wheels Build\n+    timeout-minutes: 120\n+    runs-on: linux-amd64-cpu-16\n+    container:\n+      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/triton:8.5.0-910b-ubuntu22.04-py3.11-latest\n+\n+    permissions:\n+      contents: read\n+\n+    outputs:\n+      wheel_name: ${{ steps.wheel.outputs.name }}\n+\n+    steps:\n+      - name: Checkout\n+        uses: actions/checkout@v4\n+        with:\n+          # pull_request_target: checkout PR head so we build what was proposed.\n+          # push: checkout the triggering commit.\n+          ref: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || github.sha }}\n+          fetch-depth: 0\n+          submodules: \"true\"\n+          # This workflow is scoped to contents:read only and passes zero\n+          # secrets to the build job — PR code cannot exfiltrate anything.\n+          allow-unsafe-pr-checkout: true\n+\n+      - name: Add git safe directory\n+        run: git config --global --add safe.directory \"$GITHUB_WORKSPACE\"\n+\n+      - name: Cache build dependencies\n+        uses: actions/cache@v4\n+        with:\n+          path: |\n+            ~/.triton/llvm\n+            ~/.triton/json\n+            ~/.triton/nvidia\n+            ${{ github.workspace }}/.ccache\n+          key: ${{ runner.os }}-${{ runner.arch }}-cann8.5.0-ubuntu22.04-910b-py3.11-wheels-${{ hashFiles('cmake/llvm-hash.txt', 'cmake/json-version.txt', 'cmake/nvidia-toolchain-version.json', 'third_party/ascend/patch/llvm_patch_*.patch') }}\n+          restore-keys: ${{ runner.os }}-${{ runner.arch }}-cann8.5.0-ubuntu22.04-910b-py3.11-wheels\n+\n+      - name: Build triton-ascend wheel\n+        shell: bash\n+        env:\n+          TRITON_BUILD_WITH_CCACHE: \"true\"\n+          TRITON_BUILD_WITH_CLANG_LLD: \"TRUE\"\n+          TRITON_DISABLE_LINE_INFO: 1\n+          CCACHE_COMPRESS: \"true\"\n+          CCACHE_DIR: ${{ github.workspace }}/.ccache\n+          TRITON_BUILD_WITH_O1: true\n+          TRITON_BUILD_PROTON: OFF\n+          TRITON_WHEEL_NAME: triton-ascend\n+          TRITON_APPEND_CMAKE_ARGS: \"-DTRITON_BUILD_UT=ON\"\n+        run: |\n+          mkdir -p \"$CCACHE_DIR\"\n+          ccache --zero-stats\n+          NUM_PROCS=$(( $(nproc) / 3 ))\n+          if [ \"$NUM_PROCS\" -lt 1 ]; then NUM_PROCS=1; fi\n+          # setup.py or setup_ascend.py is at repo root on main, under python/ on older release branches.\n+          if [ -f setup_ascend.py ]; then\n+            SETUP_DIR=\"./\"\n+            SETUP_PY=\"setup_ascend.py\"\n+          elif [ -f setup.py ]; then\n+            SETUP_DIR=\"./\"\n+            SETUP_PY=\"setup.py\"\n+          elif [ -f python/setup.py ]; then\n+            SETUP_DIR=\"python\"\n+            SETUP_PY=\"setup.py\"\n+          else\n+            echo \"ERROR: setup.py or setup_ascend.py not found\" >&2\n+            exit 1\n+          fi\n+          cd \"$SETUP_DIR\"\n+          MAX_JOBS=\"$NUM_PROCS\" \\\n+          python ${SETUP_PY} bdist_wheel --dist-dir=\"${GITHUB_WORKSPACE}/wheelhouse\"\n+\n+      - uses: actions/upload-artifact@v4\n+        with:\n+          name: cibw-wheels-manylinux_2_28_x86_64-wheels-upload\n+          path: ./wheelhouse/*.whl\n+\n+      - name: Resolve wheel filename\n+        id: wheel\n+        # PR build matrix produces a single wheel; capture its filename for\n+        # downstream jobs (OBS URL, BY pipeline parameter).\n+        run: echo \"name=$(basename wheelhouse/*.whl)\" >> \"$GITHUB_OUTPUT\"\n+\n+  # ── Pipeline Tests ────────────────────────────────────────────────────────\n+  # This job never checks out PR code — it only downloads the binary wheel\n+  # artifact, uploads it to OBS, and triggers the BY pipeline. Secrets (OBS,\n+  # Blue/Yellow) are safe here because no untrusted code is executed.\n+\n+  pipeline-tests:\n+    name: Pipeline Tests\n+    needs: wheels-build\n+    timeout-minutes: 360\n+    if: needs.wheels-build.result == 'success'\n+    runs-on: linux-aarch64-cpu-1\n+    container:\n+      image: swr.cn-southwest-2.myhuaweicloud.com/modelfoundry/ascend/triton:github_action\n+\n+    permissions:\n+      pull-requests: read\n+\n+    steps:\n+      - name: Resolve pipeline context\n+        id: ctx\n+        shell: bash\n+        env:\n+          GH_TOKEN: ${{ github.token }}\n+          REPO: ${{ github.repository }}\n+          EVENT_NAME: ${{ github.event_name }}\n+          PR_NUMBER: ${{ github.event.pull_request.number || '' }}\n+          HEAD_REF: ${{ github.event.pull_request.head.ref || github.ref_name }}\n+          HEAD_SHA: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || github.sha }}\n+        run: |\n+          set -euo pipefail\n+\n+          if [ \"${EVENT_NAME}\" = \"pull_request_target\" ]; then\n+            # PR context is directly available from the event payload.\n+            # We still look up the PR for the latest title (it's mutable).\n+            P=$(gh pr view \"$PR_NUMBER\" --repo \"${REPO}\" \\\n+                  --json title,baseRefName,headRefOid)\n+            PR_TITLE=$(echo \"$P\" | jq -r .title)\n+            BRANCH_B=$(echo \"$P\" | jq -r .baseRefName)\n+            OBS_KEY=\"$PR_NUMBER\"\n+            KIND=\"pr\"\n+          else\n+            # push to main / release/**\n+            PR_TITLE=\"${HEAD_REF} @ ${HEAD_SHA:0:8}\"\n+            BRANCH_B=\"$HEAD_REF\"\n+            OBS_KEY=\"$HEAD_SHA\"\n+            KIND=\"branch\"\n+          fi\n+\n+          {\n+            echo \"pr_id=${OBS_KEY}\"\n+            echo \"kind=${KIND}\"\n+            echo \"branch_b=${BRANCH_B}\"\n+            echo \"head_sha=${HEAD_SHA}\"\n+          } >> \"$GITHUB_OUTPUT\"\n+\n+          # PR_TITLE may contain newlines; pass via env file as multi-line values.\n+          {\n+            echo \"PR_TITLE<<__EOF__\"\n+            printf '%s\\n' \"$PR_TITLE\"\n+            echo \"__EOF__\"\n+          } >> \"$GITHUB_ENV\"\n+\n+      - name: Download wheel artifact\n+        uses: actions/download-artifact@v4\n+        with:\n+          name: cibw-wheels-manylinux_2_28_x86_64-wheels-upload\n+          path: wheelhouse\n+\n+      - name: Setup obsutil and upload to OBS\n+        shell: bash\n+        env:\n+          AK: ${{ secrets.OBS_AK }}\n+          SK: ${{ secrets.OBS_SK }}\n+          PR_NUM: ${{ steps.ctx.outputs.pr_id }}\n+        run: |\n+          set -eu\n+          if [ -z \"${AK}\" ] || [ -z \"${SK}\" ]; then\n+            echo \"OBS credentials are empty.\"\n+            exit 1\n+          fi\n+          curl -fL \"https://obs-community.obs.cn-north-1.myhuaweicloud.com/obsutil/current/obsutil_linux_arm64.tar.gz\" -o obsutil.tar.gz\n+          gzip -t obsutil.tar.gz || { echo \"obsutil.tar.gz is not a valid gzip\"; exit 1; }\n+          tar xzf obsutil.tar.gz\n+          rm -f obsutil.tar.gz\n+          mv obsutil_linux_* obsutil\n+          obsutil/obsutil config -i=\"${AK}\" -k=\"${SK}\" -e=https://obs.cn-southwest-2.myhuaweicloud.com\n+          obsutil/obsutil cp -u wheelhouse/*.whl \"obs://triton-ascend-artifacts/triton-ascend/${PR_NUM}/\"\n+\n+      - name: Trigger BY pipeline\n+        id: run\n+        shell: bash\n+        env:\n+          BASE_URL:           ${{ secrets.BLUE_YELLOW_BASE_URL }}\n+          APP_CODE:           ${{ secrets.BLUE_YELLOW_APP_CODE }}\n+          APP_KEY:            ${{ secrets.BLUE_YELLOW_APP_KEY }}\n+          APP_SECRET:         ${{ secrets.BLUE_YELLOW_APP_SECRET }}\n+          YELLOW_PIPELINE_ID: ${{ vars.YELLOW_PIPELINE_ID }}\n+          YELLOW_GROUP_ID:    ${{ vars.YELLOW_GROUP_ID }}\n+          PR_ID:              ${{ steps.ctx.outputs.pr_id }}\n+          URL_B:              ${{ github.server_url }}/${{ github.repository }}\n+          BRANCH_B:           ${{ steps.ctx.outputs.branch_b }}\n+          WHEEL_NAME:         ${{ needs.wheels-build.outputs.wheel_name }}\n+        run: |\n+          set -euo pipefail\n+\n+          STATUS_FILE=\"$RUNNER_TEMP/by-status\"\n+\n+          H_CODE=\"X-Apig-AppCode: ${APP_CODE}\"\n+          H_KEY=\"AppKey: ${APP_KEY}\"\n+          H_SEC=\"AppSecret: ${APP_SECRET}\"\n+          H_JSON=\"Content-Type: application/json\"\n+\n+          TS=$(date +%Y%m%d%H%M%S)\n+          BRI=\"pr-${PR_ID}-${TS}\"\n+          BTI=\"task-${PR_ID}-${TS}\"\n+\n+          write_status(){\n+            printf 'status=%s\\nmsg=%s\\nblueRecordId=%s\\nyellowPipelineUrl=%s\\n' \\\n+              \"$1\" \"$2\" \"$BRI\" \"${3:-}\" > \"$STATUS_FILE\"\n+          }\n+\n+          # JSON-escape arbitrary text (PR titles can contain quotes/newlines).\n+          PR_TITLE_J=$(printf '%s' \"${PR_TITLE}\" | jq -Rs .)\n+\n+          if [ -z \"${WHEEL_NAME:-}\" ]; then\n+            echo \"WHEEL_NAME is empty\"\n+            write_status \"START_FAILURE\" \"wheel filename not produced by build\"\n+            exit 1\n+          fi\n+          WHEEL_NAME_ENC=$(printf '%s' \"$WHEEL_NAME\" | jq -sRr @uri)\n+          OBS_URL=\"https://triton-ascend-artifacts.obs.cn-southwest-2.myhuaweicloud.com/triton-ascend/${PR_ID}/${WHEEL_NAME_ENC}\"\n+\n+          START_BODY=$(cat <<EOF\n+          {\n+            \"bluePipelineId\": \"null\",\n+            \"blueRecordId\": \"${BRI}\",\n+            \"blueRecordTaskId\": \"${BTI}\",\n+            \"blueRecordTaskName\": \"PR-${PR_ID}-CI\",\n+            \"yellowPipelineId\": \"${YELLOW_PIPELINE_ID}\",\n+            \"yellowGroupId\": \"${YELLOW_GROUP_ID}\",\n+            \"starter\": \"p_TritonCI\",\n+            \"branch\": \"main\",\n+            \"parameter\": {\n+              \"pr\": \"${PR_ID}\",\n+              \"title\": ${PR_TITLE_J},\n+              \"description\": \"${PR_ID}\",\n+              \"url_b\": \"${URL_B}\",\n+              \"branch_b\": \"${BRANCH_B}\",\n+              \"obs_url\": \"${OBS_URL}\"\n+            }\n+          }\n+          EOF\n+          )\n+\n+          # /start retry: only on gateway/network failures (502/504,\n+          # APIG.0203, curl=000); any other response is terminal. BRI/BTI are\n+          # reused across attempts so the backend can dedupe a request the\n+          # gateway lost mid-flight. --max-time 70 exceeds APIG's ~60s backend\n+          # timeout so curl waits out the 502 instead of giving up first.\n+          START_OK=false\n+          LAST_R=\"\"; LAST_BODY=\"\"\n+          DUP_RE='(exist|duplicate|already)'\n+          for ((I=1; I<=5; I++)); do\n+            R=$(curl -sS --max-time 70 \\\n+              -o /tmp/by_start.body -w '%{http_code}' \\\n+              -H \"$H_CODE\" -H \"$H_KEY\" -H \"$H_SEC\" -H \"$H_JSON\" \\\n+              -X POST \"${BASE_URL}/start\" -d \"${START_BODY}\" || echo \"000\")\n+            BODY=$(cat /tmp/by_start.body 2>/dev/null || true)\n+            LAST_R=\"$R\"; LAST_BODY=\"$BODY\"\n+            echo \"[start #${I}] HTTP=${R} response: ${BODY}\"\n+\n+            if [ \"$R\" = \"200\" ] && echo \"$BODY\" | grep -q '\"code\":200'; then\n+              START_OK=true\n+              break\n+            fi\n+            if echo \"$BODY\" | grep -Eiq \"\\\"(error_msg|msg|message)\\\":\\\"[^\\\"]*${DUP_RE}\"; then\n+              echo \"[start #${I}] backend reports duplicate task — treating as already-accepted\"\n+              START_OK=true\n+              break\n+            fi\n+            if [ \"$R\" = \"502\" ] || [ \"$R\" = \"504\" ] || [ \"$R\" = \"000\" ] \\\n+                || echo \"$BODY\" | grep -q 'APIG\\.0203'; then\n+              SLEEP=$(( I * 5 ))\n+              echo \"[start #${I}] retryable failure — sleeping ${SLEEP}s before retry\"\n+              sleep \"$SLEEP\"\n+              continue\n+            fi\n+            echo \"[start #${I}] non-retryable failure — aborting\"\n+            break\n+          done\n+\n+          if [ \"$START_OK\" != \"true\" ]; then\n+            write_status \"START_FAILURE\" \"HTTP=${LAST_R} body=${LAST_BODY}\"\n+            exit 1\n+          fi\n+\n+          QUERY_BODY=$(cat <<EOF\n+          {\"blueRecordId\":\"${BRI}\",\"blueRecordTaskId\":\"${BTI}\"}\n+          EOF\n+          )\n+\n+          ST=\"\"; MSG=\"\"; DONE=false\n+          for ((N=1; N<=720; N++)); do\n+            sleep 60\n+            QHTTP=$(curl -sS --retry 3 --retry-all-errors --retry-delay 5 \\\n+              -o /tmp/by_query.body -w '%{http_code}' \\\n+              -H \"$H_CODE\" -H \"$H_KEY\" -H \"$H_SEC\" -H \"$H_JSON\" \\\n+              -X POST \"${BASE_URL}/query\" -d \"${QUERY_BODY}\" || true)\n+            QR=$(cat /tmp/by_query.body 2>/dev/null || true)\n+            ST=$(printf '%s' \"$QR\"   | grep -o '\"status\":\"[^\"]*\"'           | head -1 | cut -d'\"' -f4 || true)\n+            MSG=$(printf '%s' \"$QR\"  | grep -o '\"msg\":\"[^\"]*\"'              | head -1 | cut -d'\"' -f4 || true)\n+            YPURL=$(printf '%s' \"$QR\" | grep -o '\"yellowPipelineUrl\":\"[^\"]*\"' | head -1 | cut -d'\"' -f4 || true)\n+            echo \"[#${N}] HTTP=${QHTTP} status=${ST:-pending} msg=${MSG}\"\n+            echo \"  body: ${QR}\"\n+            write_status \"${ST:-PENDING}\" \"${MSG}\" \"${YPURL}\"\n+            case \"${ST}\" in\n+              SUCCESS|FAILURE|MQS_SEND_FAILURE|UNAUTHORIZED|START_FAILURE|TIMEOUT|ABORT)\n+                DONE=true; break;;\n+            esac\n+          done\n+\n+          if [ \"$DONE\" != \"true\" ]; then\n+            write_status \"TIMEOUT\" \"polling exceeded 720 attempts without a terminal status\"\n+            exit 1\n+          fi\n+          echo \"terminal status: ${ST} - ${MSG}\"\n+          [ \"$ST\" = \"SUCCESS\" ] || exit 1\n+\n+      - name: Remove wheels from OBS\n+        if: always() && steps.ctx.outputs.pr_id != ''\n+        continue-on-error: true\n+        shell: bash\n+        env:\n+          AK: ${{ secrets.OBS_AK }}\n+          SK: ${{ secrets.OBS_SK }}\n+          PR_NUM: ${{ steps.ctx.outputs.pr_id }}\n+        run: |\n+          # Guard against a missing/malformed PR_NUM: an empty value would\n+          # collapse the URL to .../triton-ascend/ and `rm -rf` would wipe\n+          # every namespace. Accept a positive integer (PR number) or a full\n+          # git SHA (main-branch run).\n+          if [[ ! \"${PR_NUM:-}\" =~ ^([1-9][0-9]*|[0-9a-f]{40})$ ]]; then\n+            echo \"Refusing to delete: PR_NUM='${PR_NUM:-}' is not a PR number or SHA\" >&2\n+            exit 1\n+          fi\n+\n+          curl -fL \"https://obs-community.obs.cn-north-1.myhuaweicloud.com/obsutil/current/obsutil_linux_arm64.tar.gz\" -o obsutil.tar.gz\n+          gzip -t obsutil.tar.gz || { echo \"obsutil.tar.gz is not a valid gzip\"; exit 1; }\n+          tar xzf obsutil.tar.gz\n+          rm -f obsutil.tar.gz\n+          mv obsutil_linux_* obsutil\n+          obsutil/obsutil config -i=\"${AK}\" -k=\"${SK}\" -e=https://obs.cn-southwest-2.myhuaweicloud.com\n+          obsutil/obsutil rm -r -f \"obs://triton-ascend-artifacts/triton-ascend/${PR_NUM}/\" || true\n"
        }
      ]
    }
  ]
}