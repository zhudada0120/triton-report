name: Daily Commit Fetch & Analyze

on:
  schedule:
    # 北京时间凌晨 2:00 = UTC 18:00 (前一日)
    - cron: '0 18 * * *'
  workflow_dispatch:
    inputs:
      date:
        description: 'Target date (YYYY-MM-DD, default: yesterday Beijing time)'
        required: false
        type: string

permissions:
  contents: write

jobs:
  fetch-and-analyze:
    runs-on: ubuntu-latest
    env:
      LLM_API_BASE: ${{ vars.LLM_API_BASE || 'https://api.deepseek.com' }}
      LLM_MODEL: ${{ vars.LLM_MODEL || 'deepseek-v4-flash' }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Setup Node.js (for opencode Phase 2)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install opencode
        run: npm install -g opencode-ai

      - name: Configure opencode
        run: |
          mkdir -p ~/.config/opencode
          cat > ~/.config/opencode/opencode.json << 'EOF'
          {
            "$schema": "https://opencode.ai/config.json",
            "model": "deepseek/deepseek-v4-pro",
            "provider": {
              "deepseek": {
                "npm": "@ai-sdk/openai-compatible",
                "name": "DeepSeek",
                "options": {
                  "baseURL": "https://api.deepseek.com/v1",
                  "apiKey": "{env:OPENCODE_AUTH_TOKEN}"
                },
                "models": {
                  "deepseek-v4-flash": {
                    "name": "deepseek-v4-flash",
                    "limit": {
                      "context": 200000,
                      "output": 32768
                    }
                  },
                  "deepseek-v4-pro": {
                    "name": "deepseek-v4-pro",
                    "limit": {
                      "context": 200000,
                      "output": 32768
                    }
                  }
                }
              }
            }
          }
          EOF

      - name: Add MCP Server for opencode
        run: |
          opencode mcp add triton-report \
            -- python -m src.mcp_server_app \
            --data-dir "${{ github.workspace }}/data" \
            --ascend-repo-path "${{ github.workspace }}/repos/triton-ascend"

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Compute date (Beijing time, or from workflow input)
        id: date
        env:
          TZ: Asia/Shanghai
        run: |
          if [ -n "${{ github.event.inputs.date }}" ]; then
            echo "date=${{ github.event.inputs.date }}" >> "$GITHUB_OUTPUT"
          else
            echo "date=$(date -d yesterday +%Y-%m-%d)" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout triton source
        uses: actions/checkout@v4
        with:
          repository: triton-lang/triton
          path: repos/triton
          fetch-depth: 0

      - name: Checkout triton-ascend source
        uses: actions/checkout@v4
        with:
          repository: triton-lang/triton-ascend
          path: repos/triton-ascend
          fetch-depth: 0

      - name: Fetch triton commits
        run: >-
          python src/data/fetch_commits.py
          --repo triton-lang/triton
          --local-repo repos/triton
          --date "${{ steps.date.outputs.date }}"
          --force

      - name: Analyze triton commits (Phase 1)
        env:
          LLM_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          if [ -f "data/triton/commits/${{ steps.date.outputs.date }}.json" ]; then
            python src/data/analyze_commits.py \
              --repo triton-lang/triton \
              --date "${{ steps.date.outputs.date }}" \
              --local-repo repos/triton \
              --data-dir data \
              --force
          else
            echo "No commit data for ${{ steps.date.outputs.date }}, skipping analysis"
          fi

      - name: Deep analyze triton commits (Phase 2)
        env:
          OPENCODE_AUTH_TOKEN: ${{ secrets.OPENCODE_AUTH_TOKEN }}
        run: |
          if [ -f "data/triton/analysis/${{ steps.date.outputs.date }}.json" ]; then
            python src/data/deep_analyze_commits.py \
              --repo triton-lang/triton \
              --date "${{ steps.date.outputs.date }}" \
              --local-repo repos/triton \
              --data-dir data
          fi

      - name: Fetch triton-ascend commits
        run: >-
          python src/data/fetch_commits.py
          --repo triton-lang/triton-ascend
          --local-repo repos/triton-ascend
          --date "${{ steps.date.outputs.date }}"
          --force

      - name: Analyze triton-ascend commits
        env:
          LLM_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          if [ -f "data/triton-ascend/commits/${{ steps.date.outputs.date }}.json" ]; then
            python src/data/analyze_commits.py \
              --repo triton-lang/triton-ascend \
              --date "${{ steps.date.outputs.date }}" \
              --local-repo repos/triton-ascend \
              --data-dir data \
              --force
          else
            echo "No commit data for ${{ steps.date.outputs.date }}, skipping analysis"
          fi

      - name: Build search index
        run: python src/data/build_index.py --data-dir data

      - name: Track adaptation
        run: |
          if [ -f "data/triton-ascend/adaptation-status.json" ]; then
            python src/data/track_adaptation.py detect --ascend-repo-path repos/triton-ascend --data-dir data
          else
            python src/data/track_adaptation.py init --ascend-repo-path repos/triton-ascend --data-dir data
          fi

      - name: Clean stale data (commits without analysis)
        run: python .github/scripts/clean_stale_data.py --data-dir data

      - name: Commit and push changes
        run: >-
          git config user.name "github-actions[bot]"
          && git config user.email "github-actions[bot]@users.noreply.github.com"
          && git add data/
          && git diff --staged --quiet
          || (git commit -m "chore: update data $(TZ=Asia/Shanghai date +%Y-%m-%d)"
          && git push)